# CHANGELOG

One line per spec, newest first.

- **spec-04** — Activity selection and focus: migration 0006 adding an `activity_kind` enum (`recurring_community` | `one_off_source`), `activities.kind`, an advisory `activities.fit_score` and `profiles.focus_cap` (2–4, default 3); a real `activity_suggestion` prompt with input widened to carry traits, the persona's own activities and the five constraint answers, and output gaining `kind` and `fit_score`; a pure plan engine (`lib/activities/plan.ts`) owning seeding, idempotent merging, the derived focus set and cap enforcement; an activities page with rationale-carrying cards, status and kind controls, add-your-own, an editable cap and a "this season's plan" section; `activities_selected` added to the onboarding sequence for spec 05 to gate on. Also spec 12a item 2's assessment Playwright suite (`e2e/assessment.spec.ts`), which seeds the interview and needs no model calls.
- **spec-03** — Assessment interview: a four-inventory catalogue with original wording and pure, deterministic scoring (`lib/assessments/catalogue.ts`); real prompts for the `interview` and `persona_synthesis` components, with the interview gaining `input_kind`, `choices`, a question budget and catalogue-restricted inventory suggestions; a stateless flow engine (`lib/assessments/flow.ts`) that derives the interview's position from the stored answers alone; a one-question-at-a-time UI with a progress bar, Back-and-edit, per-answer writes and a real error plus Retry on gateway failure; persona synthesis and a results page showing the scored inventories and the persona, with per-section redo and regeneration that keeps every earlier assessment. `assessments.desired_activities` widened from `string[]` to `{name, rationale}[]` (jsonb, no migration).
- **spec-12a** — Quality gates, pulled forward ahead of spec 03: GitHub Actions running lint, `tsc` and vitest on every push and PR with a README badge; a Playwright end-to-end login test (admin-minted magic link → the app's own `/auth/callback` → authenticated `/settings`) against a production build, skipping cleanly in CI until the Supabase repository secrets are added; husky + lint-staged pre-commit hook. Assessment/event-selection e2e tests and Lighthouse CI deferred until specs 03 and 07 exist.
- **spec-02** — LLM gateway (`lib/llm/gateway.ts`) as the single entry point for every model call, with Zod validation, one corrective retry and a `run_log` row per run; component registry with schemas and stub prompts; Settings page with encrypted provider keys, live per-component model dropdowns, run log with cost/latency and rerun-with-another-model; model setup as onboarding step 1, gating the assessment.
- **spec-01** — Next.js App Router scaffold with Tailwind and a PWA shell; Supabase magic-link auth gated in `proxy.ts`; all 15 tables from the data model with row-level security; Zod schemas and tests for every table; eight-section app shell.
- **spec-00** — Environment setup: Supabase project `wqawpwbgrsjusbdopgbi`, GitHub repo `emanstof-png/social-app`, Vercel project `gazelle`. Gemini added as a supported provider. No code.
