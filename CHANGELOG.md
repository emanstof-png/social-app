# CHANGELOG

One line per spec, newest first.

- **spec-18** — Assessment runs (PRD §1.3-1.5): every interview is now a run (`assessment_runs`, migration 0019, with `assessment_answers.run_id`/`assessments.run_id` and a backfill giving every existing user's rows a run), not a single flat set of answers per user. `lib/assessments/runs.ts`'s `currentRun` (newest by `started_at`, no status column — created when the user has none) scopes every read/write in `app/(app)/assessment/actions.ts`, `app/(app)/activities/data.ts`, and `app/(app)/settings/page.tsx`'s dial defaults. A new "Start a new assessment" action + confirm dialog (`app/(app)/confirm-dialog.tsx`, a new `docs/CONVENTIONS.md#dialogs` entry) starts a fresh run without touching `onboarding_state` or deleting anything; the results page gained a "Previous assessments" history section reading every other run's own finished assessment and transcript back. `activities.assessment_id` (nullable) gives seeded rows provenance, showing a quiet "From your \<date\> assessment" line on a row the current run introduced. Verified live, not just `next build`: a real production `next start` server served real authenticated requests — `npx playwright test e2e/assessment.spec.ts` (4/4, including two new spec 18 tests) and the full `npm run test:e2e` suite (33 passed, 1 pre-existing skip) both against real installed Chrome. Tagged `spec-18`, not pushed (`loop.config.json`'s `push` is `false`).
- **spec-16** — Feed calendar and event summaries (PRD §2.5): `events.description` (migration 0018, nullable text) is filled at scrape time only — `lib/scraping/ics.ts`'s `DESCRIPTION` mapping (unescaped, collapsed to single spaces, truncated to 280 characters at a word boundary) or a new `event_extraction` prompt rule riding the existing per-page call (no new LLM component, no new model call) — never by the user, and `lib/scraping/plan.ts#mergeEvents` writes it into `WRITABLE` so a re-scrape backfills it on rows written before this spec. `/feed`'s `Card` renders it under the community/time line, clamped to two lines, rendering nothing at all when null. The month grid moved out of `calendar-view.tsx` into a shared `app/(app)/feed/month-grid.tsx` (moved, not reimplemented) that both `/feed` (unfiltered day markers, new `?month=` param) and `/calendar` (`committedOnly`, unchanged) now import — explicitly not a repeat of the reverted `01e859d` merge, since the two pages still differ by which card set they hand the shared grid and their own list. Verified live, not just `next build`: a real production `next start` server served real authenticated requests exercising both `/feed` and `/calendar`. Tagged `spec-16`, not pushed (`loop.config.json`'s `push` is `false`).
- **spec-15-review-fixes** — resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 15's review gate: its two CI-requiring acceptance criteria were structurally unreachable by any build session under `loop.config.json`'s `push: false` (no build session ever pushes, and CI only runs on a push). No code change: Eric had the manager push `spec-15` directly and read two real CI runs, transcribed honestly into `REVIEW.md`'s new "CI confirmation" section. First run: the Playwright job's real steps were all skipped (two missing repository secrets, unrelated to spec 15's code). Second run, after Eric restored both secrets: the job actually executed — `google-chrome --version` printed a real version, but `e2e/settings-push.spec.ts`'s real-GCM-handshake test failed after exhausting its two retries (28 passed / 1 failed / 1 skipped), a real instance of the exact CI-environment risk spec 15's own Risks section already flagged as credible and unconfirmed. Both CI-requiring acceptance criteria remain open, honestly, pending a green run or three consecutive failures triggering the spec's own documented fallback. `STATUS.md`'s Waiting on Eric section updated to match. Tagged `spec-15-review-fixes`, not pushed (`loop.config.json`'s `push` is `false`).
- **spec-15** — e2e suite on a real installed Chrome (no PRD coverage, test infrastructure like specs 12a/13/14): `playwright.config.ts`'s `chromium` project gains `channel: "chrome"`, targeting a real branded Google Chrome install rather than Playwright's bundled open-source Chromium, which has no Google API key and so cannot complete real GCM registration (spec 09's own finding). New `e2e/fixtures.ts` overrides the built-in `context`/`page` fixtures with a fresh `launchPersistentContext` per test against a temp `os.tmpdir()` profile directory, reproducing today's `trace: "on-first-retry"` behavior by hand since overriding `context` takes over that responsibility; every spec file except `cron-evaluation-prompts.spec.ts` (which only uses the `request` fixture and never launches a browser) now imports `test`/`expect` from `./fixtures` instead of `@playwright/test` directly — one line each, no assertion or selector changes. `e2e/settings-push.spec.ts` gains one new assertion — the created row's `endpoint` must actually be a real GCM/FCM `https://` URL, not merely "no error was thrown" — plus, per `NEEDS_HUMAN.md`/GitHub issue #10's resolution (option 3, a bounded retry not a skip), its own `describe` block sets `test.describe.configure({ retries: 2, timeout: 90_000 })`, scoped to that file only, since the real browser-level `pushManager.subscribe()` GCM/FCM handshake sometimes does not resolve within 15-20s even though `subscribeToPush` itself is correct; after all retries the test still fails outright, confirmed by deliberately breaking the assertion locally and watching it retry to "Retry #2" before failing. CI's `e2e` job drops `npx playwright install --with-deps chromium` (nothing launches a Playwright-managed browser anymore) and gains a `google-chrome --version` step so a runner missing system Chrome fails loudly there, not inside an oblique test failure — relying on `ubuntu-latest`'s preinstalled Chrome rather than a `playwright install chrome` step, which does not reliably provision anything on Linux. `docs/CONVENTIONS.md#tests` and `docs/ARCHITECTURE.md`'s "Evaluation and push" section both record the fix. Verified live, not just `next build`: the full `npm run test:e2e` suite passed against a real production `next start` server driven by a real installed Chrome, including `settings-push.spec.ts`'s previously-failing real-subscribe test; `npx playwright test e2e/settings-push.spec.ts` twice back to back both passed, proving per-test profile isolation; no `gazelle-e2e-*` temp directories remained under `os.tmpdir()` after the run.
- **spec-10** — CRM: `app/(app)/people/` (spec 04/05 five-file shape) reuses the `contacts`/`interactions`/`invite_suggestions` tables and Zod schemas spec 01 already shipped unused — no new migration. `createContact` writes exactly one founding `'met'` interaction per contact; `updateContact` mirrors `updateCommunity`'s per-field patch shape; `logInteraction` accepts only `'text' | 'invite' | 'hangout'` (narrower than the column's own enum, since `'met'` is written in exactly one place); tallies (`people/view.ts#tally`) are computed live from `interactions`, never stored. `lib/crm/import.ts` hand-rolls vCard and CSV parsers (no new dependency, same call `lib/scraping/ics.ts` made) behind a parse-only `previewImport`/write `importContacts` pair, flagging a likely-duplicate name for a person to decide. The compose panel drafts a fixed, non-LLM template behind `sms:`/`mailto:` links and a manual "I sent this" confirmation (CLAUDE.md: the app only prepares, never sends) — `invite_suggestion`'s real prompt stays spec 11's. The viral/k-factor loop from `docs/specs/10-crm-addition-note.md` is deliberately not folded in; flagged here for the review gate instead. A real bug found live (not by the unit suite): the import review panel unmounted its own "Imported N contacts" confirmation in the same tick it appeared — the same unmount-before-render class specs 04/07/09 each hit once — fixed by not auto-closing on success. Verified live: `e2e/people.spec.ts` (8/8) against a real production `next start` server and the real database — add-with-founding-interaction, per-field edit isolation, archive/restore, tally freshness after a fresh render, compose-panel hrefs and confirm-send counts, vCard/CSV import (missing-FN warning, duplicate flag), and the client-side search filter with no server round trip. Tagged `spec-10`, not pushed (`loop.config.json`'s `push` is `false`).
- **spec-14-review-fixes** — Spec 14 review-fixes addendum: resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 14's review gate — the `npm run loop:once`-with-`logs/live.log`-watched acceptance criterion had no evidence, since the sandboxed builder that finished spec 14 couldn't invoke bare `claude` itself. No code change: this addendum's own session, launched by `scripts/run-spec.sh` as a genuine `loop:once` iteration, directly observed `logs/live.log` grow from 13 to 26 real lines as its own real tool calls happened, including a genuine `planner`-role phase from the same iteration preceding it. `REVIEW.md` folds this evidence back into spec 14's own account. Tagged `spec-14-review-fixes`, not pushed (`loop.config.json`'s `push` is `false`).
- **spec-14** — Live loop log, loop tooling only (no PRD coverage, like spec 13): `scripts/loop-live.ts` sits in the pipe between each `claude -p --output-format stream-json --verbose` invocation and `$LOG_FILE` in `scripts/run-spec.sh`, appending one formatted line per tool call or text message (`HH:MM:SS  <role>  <ToolName>  <detail>`) to a new `logs/live.log` while echoing every raw input line through unchanged, so `logs/run-spec-YYYYMMDD.log` keeps carrying the exact same raw NDJSON it always has. `logs/live.log` truncates fresh at the start of every `run-spec.sh` invocation. Never exits non-zero (a crash here would otherwise be misread as `claude`'s own exit code under `run-spec.sh`'s `set -o pipefail`, breaking halt detection). Item 3's required process-group-kill proof under the new pipe hit the `.claude/settings.json` allowlist's own High-tier stop (no bare `bash`/`ps`/`kill`, no bare `claude`) and was resolved by Eric running it by hand with a fake `claude` on PATH: `run_with_timeout` still returns 124 and leaves no surviving `claude`, `npx tsx`/`node`, or child `claude` process with `loop-live.ts` in that same real pipe. A real `npm run loop:once` run against live `claude` sessions hits that identical allowlist wall for a builder session, so this session instead drove the real `scripts/loop-live.ts` process directly over streamed stdin (not `formatEvents()` in isolation): `logs/live.log` grew from 0 to 37 bytes after the third of four fixture lines while the child process was still alive, confirming lines land on disk as each event is processed rather than only at exit.
- **spec-09-review-fixes** — Spec 09 review-fixes addendum: `e2e/evaluations.spec.ts` gained two parametrized tests (`'cut'`, `'returning'`) proving `submitEvaluation`'s cut/returning guard never promotes an already-cut or already-returning community to `'returning'` on a liked visit, while `times_visited` still increments — the one `blocking` finding from spec 09's review gate. No code change to the guard itself, confirmed correct as written. Verified live: `npx playwright test e2e/evaluations.spec.ts` 4/4 against a real production `next start` server; the full `test:e2e` suite run as a regression check shows only the two pre-existing, already-documented gaps (Chromium's Push API restriction, `CRON_SECRET` unset). Tagged `spec-09-review-fixes`, not pushed (`loop.config.json`'s `push` is `false`).
- **spec-09** — Evaluation and push: `evaluations` (migration 0017) gains `occurrence_at` and a widened unique key; `app/(app)/evaluations/` (the spec 04/05 five-file shape) asks whether a passed, planned occurrence was attended and liked, writing the evaluation, flipping `selections.status` to `attended`/`skipped`, and — only when attended — incrementing `communities.times_visited` and advancing `status` to `returning` (liked, and only from `todo`/`went_once`) plus two `preference_log` rows, all through existing per-field patterns. `web-push` (new runtime dependency, pre-approved in the spec) backs `lib/push/notification.ts`/`webpush-server.ts` and a Settings subscribe/unsubscribe UI (`push_subscriptions`, migration 0016); `public/sw.js` gained `push`/`notificationclick` handlers. `app/api/cron/evaluation-prompts/route.ts` (`CRON_SECRET`-gated, `/api/cron` added to `PUBLIC_PATHS`) sends the actual prompts once daily (`vercel.json`), stamping `selections.evaluation_prompted_at` as its one-way idempotency marker. `/settings` shows a "Liked N of M recent visits" hint next to "Find more communities," a read with no automatic trigger. A real production-only bug (the Evaluations page's own confirmation UI unmounted by its own `revalidatePath`) was found and fixed by the required live e2e run — `EvaluationsView` now freezes its render list at mount instead of resyncing from the prop. Verified live: `npm run test:e2e` 18/19 (1 skipped: the cron route's positive path, blocked on `CRON_SECRET`, not yet set anywhere). Not verified live: the push subscribe/unsubscribe round trip, which cannot be driven by Playwright's bundled Chromium in any environment (confirmed via two isolated diagnostics — incognito-mode Push API restriction, then a missing Google API key even in a persistent context) — see `REVIEW.md` for the resulting decision this raises for later specs' own e2e coverage. Tagged `spec-09`, not pushed (`loop.config.json`'s `push` is `false`).
- **spec-08** — Google Calendar sync: `google_accounts` (migration 0015) holds one encrypted OAuth connection per user (`encryptSecret`/`decryptSecret`, reused from `provider_keys`); `selections` gains `gcal_sync_status`/`gcal_sync_error_kind`/`gcal_sync_error_message`, reusing `run_status`/`run_error_kind` verbatim. `lib/google/oauth.ts`/`oauth-server.ts` and `calendar.ts`/`calendar-server.ts` follow the gateway's pure/impure pair pattern, with exactly one refresh-and-retry on a 401; `app/auth/google/callback/route.ts` mirrors the existing Supabase auth callback. `selectOccurrence`/`unselectOccurrence` (`app/(app)/feed/actions.ts`) sync synchronously when an account is connected and leave every sync column null (no error shown) when one is not; a sync failure never rolls back the local `selections` write, and a Retry control (`retryGoogleSync`) re-attempts it. `app/(app)/settings/google-calendar.tsx` adds connect/disconnect/check-connection to Settings. Verified live, not just `next build`: the full `npm run test:e2e` suite (14/14) passed against a real production `next start` server, including a new test that drives a real (deliberately invalid) round-trip against Google's actual API. Not yet done: the spec's own required live hand-test with a real connected account, blocked on three Google Cloud Console steps only Eric can do — see `STATUS.md` and `REVIEW.md`. Tagged `spec-08` and pushed.
- **git-push-follow-tags** — Housekeeping fix found while pushing spec-08: `git push --follow-tags` only forwards annotated tags, and every `spec-NN` tag in this repo is lightweight, so it silently pushed the branch and left the tag off `origin`. `scripts/run-spec.sh`'s own automated push path was already correct (an explicit `git push origin "spec-$spec_num"`); only the human-facing recommendation was wrong, in three places — `loop.config.json`'s `push` comment, `docs/ARCHITECTURE.md`'s Build loop section, and `run-spec.sh`'s `push=false` log message — all now say `git push origin <branch> spec-NN` instead. No code path changed.
- **saved-confirmation-fix** — Fix against the spec-07-calendar-fields addendum, built directly outside the loop: the Community card's autosaved fields (`app/(app)/communities/communities-view.tsx`) now flash a "✓ Saved" `role="status"` badge next to whichever field just wrote successfully, tracked per-field so editing one never flashes another's confirmation — previously a success was silent, indistinguishable from a write that never fired. Applied to all five save-backed fields (status, times visited, rating, focus, notes), since they share one `save()` function. A same-session attempt to also merge `/calendar` into `/feed` as one page (PRD §2.5) was built and then fully reverted at Eric's request after review (`git revert` of the merge commit, itself committed) — `/calendar` stands exactly as `spec-07-calendar-fields` shipped it, below. Verified live, not just `next build`: the full `npm run test:e2e` suite (12/12, the same set `spec-07-calendar-fields` shipped plus the new Saved-badge test) passed against a real production `next start` server.
- **spec-07-calendar-fields** — Calendar/community-fields addendum (`docs/specs/07-calendar-and-community-fields-addendum.md`), settled at the spec 07 review gate from a live hand-test: `/calendar` is now committed-only — `lib/feed/occurrences.ts#committedOnly` filters `loadFeedData`'s cards to `selections.status IN ('planned', 'attended')` for both the Upcoming list and the month grid's day markers, applied in `app/(app)/calendar/page.tsx`; `/feed` is unchanged. Clicking a day in the month grid (`calendar-view.tsx`) scrolls/highlights the matching Upcoming section, or, for a day with nothing committed, scrolls to the nearest committed day via a new `nearestDay` helper and shows a notice — a click never silently no-ops. `communities` (migration 0014) gains `times_visited` (int, default 0) and `rating` (1-5, nullable), both user-owned and manually editable on the Community card next to `status`, written independently through `updateCommunity`'s existing per-field-patch pattern; spec 09's evaluation flow will later write them from real attendance. Verified live, not just `next build`: migration 0014 applied via `npm run migrate` and confirmed via `migrate:status`; the full `npm run test:e2e` suite (12/12, including three new tests — committed-only filtering, the day-click scroll/nearest-day/notice behavior, and independent times-visited/rating writes surviving a reload) passed against a real production `next start` server under real authenticated requests.
- **spec-03-rework** — Assessment rework addendum (`docs/specs/03-assessment-rework-addendum.md`): about-you (`lib/assessments/catalogue.ts#ABOUT_YOU_QUESTIONS`) replaces the LLM-paced hobbies/desires phases with one static section folding in the original constraint questions, leaving exactly one model call in the interview — a single `interview` request to choose the phase-B inventories, absorbed inline by `currentQuestion` so the UI never sees it as a step; `submitAnswer` fires `persona_synthesis` in a new `after()` callback the instant the interview completes and returns immediately, a genuinely new pattern documented at `docs/CONVENTIONS.md#background-work-after-the-response` — the results view (`app/(app)/assessment/data.ts#loadResultsPhase`, `generating.tsx`) polls for the `assessments` row, distinguishing "still running" from "failed" by checking for a newer `run_log` error row with no matching result, per its own required failure handling. `profiles` (migration 0013) gains five nullable Settings dials (`dial_budget`/`sobriety`/`physical`/`location`/`schedule`) overriding the matching assessment answer once touched (`constraintsFrom` in `lib/activities/plan.ts`), editable on `/settings` (`dials.tsx`) with "Find more activities"/"Find more communities" re-running the existing spec 04/05 actions rather than new logic. `/assessment` adopts the spec 04/05 five-file page-layout split (`data.ts` added), closing the `CONVENTIONS.md` pre-convention note. Verified live against the real gateway: `after()` fires post-response with no page freeze, a real model failure (this account's pre-existing, `STATUS.md`-documented dead free-tier model) wrote the expected `run_log` error row and rendered the failure/Retry UI with no assessments row written — the success path is covered by unit tests (`tests/assessment-data.test.ts`) and the existing e2e suite's direct-seed results test, not observed live on this account.
- **spec-07** — Feed and calendar views: `lib/feed/occurrences.ts` expands a scraped event's RRULE (or leaves free-text/absent recurrence as one card) into dated occurrences at read time, over a 90-day window capped at 26 instances per event; migration 0012 widens `selections`' unique key to `(user_id, event_id, occurrence_at)` so one recurring event can have more than one independent selection; `app/(app)/feed/` (`loadFeedData`, `selectOccurrence`/`unselectOccurrence`) is the one read/write path both the Feed's flat chronological card list and `/calendar`'s month-grid-plus-day-list share, the latter with no `data.ts`/`actions.ts` of its own; `e2e/feed.spec.ts` seeds a fixture event and drives a real Select/unselect against the real database. A real bug found live by that test, before it ever passed: the Feed/Calendar never showed a selection that genuinely existed in the database, because the read-side join keyed occurrences by two textually different (but instant-equal) forms of the same timestamp — fixed with one shared `occurrenceKey` helper that normalizes through `Date` before comparing.
- **spec-13** — Autonomous runner, built out of order right after spec 06 for the same reason 12a jumped the queue: three agent prompts (`docs/agents/PLANNER.md`/`BUILDER.md`/`REVIEWER.md`) for headless `claude -p` sessions, replacing the old per-item "continue" checkpoint with `CLAUDE.md`'s tier rule (Low builds straight through, Medium proceeds with red-before-green tests and a `REVIEW.md` flag, High writes `NEEDS_HUMAN.md` and stops); `scripts/needs-human.ts` writes that file and opens a matching GitHub issue using `GH_TOKEN`; a committed `.claude/settings.json` allowlist (`npm run *`, `npx supabase *`/`tsx *`/`vitest *`/`playwright *`, `git *` except force-push/reset-hard) for unattended `--permission-mode acceptEdits` sessions; `scripts/run-spec.sh` (`npm run loop`/`loop:once`) loops halt-check → pull → plan → (stop on a fresh draft, or) build under a wall-clock cap → wait for CI → review, with a portable polling-based timeout standing in for GNU `timeout`. The Supabase CLI replaces hand-applying migrations, linked to `wqawpwbgrsjusbdopgbi` with 0001–0011 reconciled into its tracking table. Verified live: `npm run migrate`/`migrate:status` against the real project with a throwaway migration, a real GitHub issue opened and closed by `needs-human.ts`, a real `curl` denied by the allowlist, and six fixture-based scenarios exercising `run-spec.sh` itself (two real bugs in the timeout mechanism found and fixed by that testing). Not done: the spec's own required live `loop:once` run on a real spec (07), which the auto-mode classifier correctly declined to let a nested session kick off unsupervised — handed to Eric via `NEEDS_HUMAN.md`.
- **spec-06** — Calendar scraping: `lib/scraping/plan.ts`'s `scrapeCommunity` decides ics/api/html/null per community and runs one pass — no round engine, since a calendar is one feed or one page fetched once, not an open-ended search; `lib/discovery/calendar-kind.ts`/`-server.ts` detect the kind (a known Meetup/Eventbrite host classifies without a request; migration 0011 adds `calendar_kind_checked_at` so an unreachable calendar is not re-probed every page load); `lib/scraping/ics.ts` is a minimal RFC 5545 `VEVENT` parser (unfolding, TEXT unescaping, `DATE`/`DATE-TIME` via `Intl`, `RRULE` captured raw and never expanded, a malformed block skipped without losing the rest of the feed); `event_extraction` got its real prompt (never invents time/price/location, one event with `recurrence` for a repeating pattern rather than an invented series) and `discovery_extraction`'s default model with the same reasoning; `hashKeyFor` is the one place `dedupe_hash` is computed, and `source_url` is stamped by `plan.ts` alone from the URL actually fetched, never the model or an ICS `VEVENT`'s own `URL` (mapped to `rsvp_url` instead); a "Find events" button on the Communities page. Item 0: the e2e suite's account (`e2e+gazelle@example.com`) turned out to hold the real discovery data all along — build sessions since spec 02 used its admin-minted magic link as a stand-in for "a real authenticated request," and spec 05's real run wrote the 9 real communities to it, which `resetUser` then deleted on every e2e run. Migrated that data to the actual account (`emanstof@gmail.com`) and pinned the e2e suite to a separate, disposable `E2E_USER_ID`. Verified live: a real HTML scrape wrote 4 real events with correctly DST-adjusted times; the required gateway-failure path fired unprompted when this account's `event_extraction` model turned out to be a since-discontinued OpenRouter free-tier slug. No real `.ics` calendar exists among the current real data, so the ICS path is verified by tests only, not live.
- **spec-05-dedupe** — The duplicate-names addendum (`docs/specs/05-duplicate-names-addendum.md`), built as item 0 of the spec 06 session: `mergeFindings` now matches on a `matchKey` that strips a leading "the", a trailing letters-only parenthetical acronym and separator punctuation, plus a second key on the full `normalizeUrl(website)` (never the host, so two dances under one parent society's site stay two rows); name key first, website key second, and a disagreement between them takes the name match and reports it on a new `MergePlan.ambiguous` rather than resolving it silently. `nameKey` is unchanged and still describes what `communities_user_name_key` enforces. Corrected at its own review gate: `normalizeUrl` in `lib/search/chain.ts` now strips a leading `www.` (the exact label only), which is what finally joins `fsgw.org/...` and `www.fsgw.org/...` as one organization and, in the search chain that shares the key, stops the loop reading one page twice. No migration, no new dependency.
- **spec-05** — Community discovery: a search-provider chain (`lib/search/`) mirroring the model gateway — Exa → Tavily → Serper, skipping any provider without a key, falling through on 429/402/auth/5xx/timeout, stopping on the first that answers including one that answers with zero results, and writing one `search_log` row per attempt; Gemini's Google Search grounding removed entirely (`ChatRequest.googleSearch`, the `google_search` tool push and the gateway flag), with `discovery_research` rewritten from returning communities to planning and critiquing rounds and a new `discovery_extraction` component turning one fetched page into organizations; a deterministic round engine (`lib/discovery/research.ts`, `round.ts`, `budget.ts`) where the empty-round rule is enforced twice — a schema minimum of 3 queries so an empty list fails the component's own output schema, and a `roundOutcome` that keeps `empty` (re-query) strictly apart from `failed` (stop and surface the provider message); robots.txt honoured with an unreachable file counting as disallowed and a disallowed page never substituted with provider-returned content; a Communities page gated on `activities_selected` advancing one round per request with resumable runs, source-URL links on every card, and editable status/focus/notes that discovery never writes; `scripts/discover.ts` running the whole loop against the real APIs and writing nothing. Migrations 0008 (enums), 0009 (`discovery_runs`, `search_log`, four discovered-fact columns on `communities`) and 0010 (`discovery_runs.pages_seen`, needed once each round became a separate request). `tsx` added as a devDependency.
- **spec-04** — Activity selection and focus: migration 0006 adding an `activity_kind` enum (`recurring_community` | `one_off_source`), `activities.kind`, an advisory `activities.fit_score` and `profiles.focus_cap` (2–4, default 3); a real `activity_suggestion` prompt with input widened to carry traits, the persona's own activities and the five constraint answers, and output gaining `kind` and `fit_score`; a pure plan engine (`lib/activities/plan.ts`) owning seeding, idempotent merging, the derived focus set and cap enforcement; an activities page with rationale-carrying cards, status and kind controls, add-your-own, an editable cap and a "this season's plan" section; `activities_selected` added to the onboarding sequence for spec 05 to gate on. Also spec 12a item 2's assessment Playwright suite (`e2e/assessment.spec.ts`), which seeds the interview and needs no model calls. Corrected at the review gate by migration 0007 (`activities.kind_edited_by_user`): a suggestion re-run no longer overwrites a `kind` a person set by hand, while still correcting a guess nobody has reviewed.
- **spec-03** — Assessment interview: a four-inventory catalogue with original wording and pure, deterministic scoring (`lib/assessments/catalogue.ts`); real prompts for the `interview` and `persona_synthesis` components, with the interview gaining `input_kind`, `choices`, a question budget and catalogue-restricted inventory suggestions; a stateless flow engine (`lib/assessments/flow.ts`) that derives the interview's position from the stored answers alone; a one-question-at-a-time UI with a progress bar, Back-and-edit, per-answer writes and a real error plus Retry on gateway failure; persona synthesis and a results page showing the scored inventories and the persona, with per-section redo and regeneration that keeps every earlier assessment. `assessments.desired_activities` widened from `string[]` to `{name, rationale}[]` (jsonb, no migration).
- **spec-12a** — Quality gates, pulled forward ahead of spec 03: GitHub Actions running lint, `tsc` and vitest on every push and PR with a README badge; a Playwright end-to-end login test (admin-minted magic link → the app's own `/auth/callback` → authenticated `/settings`) against a production build, skipping cleanly in CI until the Supabase repository secrets are added; husky + lint-staged pre-commit hook. Assessment/event-selection e2e tests and Lighthouse CI deferred until specs 03 and 07 exist.
- **spec-02** — LLM gateway (`lib/llm/gateway.ts`) as the single entry point for every model call, with Zod validation, one corrective retry and a `run_log` row per run; component registry with schemas and stub prompts; Settings page with encrypted provider keys, live per-component model dropdowns, run log with cost/latency and rerun-with-another-model; model setup as onboarding step 1, gating the assessment.
- **spec-01** — Next.js App Router scaffold with Tailwind and a PWA shell; Supabase magic-link auth gated in `proxy.ts`; all 15 tables from the data model with row-level security; Zod schemas and tests for every table; eight-section app shell.
- **spec-00** — Environment setup: Supabase project `wqawpwbgrsjusbdopgbi`, GitHub repo `emanstof-png/social-app`, Vercel project `gazelle`. Gemini added as a supported provider. No code.
