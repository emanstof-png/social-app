# NEEDS HUMAN: spec 14 item 3

## What is needed
Run scope item 3's own required live test by hand: prove run_with_timeout still kills the whole process group now that the builder's claude invocation pipes through 'npx tsx scripts/loop-live.ts'. This needs bare bash/ps/kill, none of which .claude/settings.json's allowlist covers (only 'npm run *', 'npx supabase|tsx|vitest|playwright *', and 'git *' are allowed) -- every such command this session tried came back refused. Recipe: on a throwaway branch, write a fake 'claude' executable earlier on PATH that prints one stream-json-shaped line then forks a background 'sleep 300 &' and itself sleeps 300s (simulating a long turn plus a child claude itself spawned); with 'set -m' in effect, run 'run_with_timeout 5 bash -c "claude -p x --permission-mode acceptEdits --output-format stream-json --verbose 2>&1 | npx tsx scripts/loop-live.ts"' (paste scripts/run-spec.sh's run_with_timeout function, unchanged, into the test shell first); confirm it returns 124, then use ps to confirm no claude, no npx tsx/node running loop-live.ts, and no leftover sleep 300 survive. Once confirmed, note the result in REVIEW.md, delete NEEDS_HUMAN.md, commit, and restart the loop so the builder finishes item 3's REVIEW.md flag plus item 4 (docs).

## What the session did before stopping
Items 1 (fixture, already captured by hand and resolved before this session started) and 2 (scripts/loop-live.ts plus tests/loop-live.test.ts, 13/13 passing, committed) are done. Item 3's code -- wiring --output-format stream-json --verbose and the pipe through loop-live.ts into all three claude -p call sites in scripts/run-spec.sh, plus truncating logs/live.log once per invocation -- is written and committed (commit f885398), but its own required live proof (the ps-based process-group-kill check) could not be run: every bare bash/ps/kill command this session attempted was refused by .claude/settings.json's allowlist. Item 4 (docs) was not started, per CLAUDE.md's rule that one High-tier stop halts the whole spec rather than skipping ahead.

## Written by
`scripts/needs-human.ts`, 2026-09-12T19:32:12.445Z
