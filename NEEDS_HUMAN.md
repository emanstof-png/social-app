# NEEDS HUMAN: spec 19 item 1

## What is needed
A decision on how to fix e2e/fixtures.ts's context fixture: either (a) approve removing the manual context.tracing.start()/stop() calls entirely, since Playwright 1.63.0's own automatic per-context trace-chunk hook (_startTraceChunkOnContextCreation in node_modules/playwright/lib/index.js) already starts/stops tracing on any newly-created BrowserContext -- including one created directly via chromium.launchPersistentContext, bypassing the built-in context fixture entirely -- whenever trace capture is needed for the attempt (on-first-retry mode fires this at testInfo.retry===1), making the fixture's own manual calls a duplicate that throws 'Tracing has been already started' when both fire on the same retry; or (b) some other resolution Eric prefers (e.g. detecting whether trace capture is already active before calling start by hand). Confirmed empirically: with the manual tracing.start()/stop() calls removed from a throwaway copy of the fixture and only the try/finally close/cleanup kept, a deliberately-throwing test's retry #1 got a genuinely fresh context and failed on its own merits with an automatically-generated trace.zip attached -- with the manual calls left in place (spec 19's literal prescribed fix: wrap the existing calls in try/finally, changing nothing else), the identical retry #1 still throws 'Tracing has been already started' at fixtures.ts:38, unchanged from the reported bug.

## What the session did before stopping
Built spec 19 item 1's literal prescribed fix (wrapped e2e/fixtures.ts's await use(context) in try/finally around the existing teardown, changing nothing about what the teardown does, exactly as the spec's Decision 3 requires) and reproduced the before/after behavior locally with npx playwright test <file> --retries=1 per the spec's own test recipe. The 'before' run (unmodified fixture) failed retry #1 with 'Error: tracing.start: Tracing has been already started' at fixtures.ts:38, matching the CI-observed bug exactly. The 'after' run (try/finally applied, otherwise unchanged) failed retry #1 with the IDENTICAL error and location -- the prescribed fix does not close the acceptance criterion ('a genuinely fresh context -- no tracing.start...already started error'). Investigated further by reading node_modules/playwright/lib/index.js and confirmed Playwright's own automatic trace-chunk-on-context-creation hook now applies to any new BrowserContext regardless of which fixture created it, not just the built-in context fixture as fixtures.ts's own header comment (written for spec 15) assumed -- so on the first retry, both Playwright's automatic hook and the fixture's manual call try to start tracing on the same fresh context, and the second one throws. This means the spec's own diagnosis (a leaked context from skipped teardown) is not what's producing the CI failures; the collision is between the fixture's manual tracing calls and Playwright's own automatic instrumentation, and would happen on every first retry of every test regardless of whether teardown ran. Left the try/finally change in e2e/fixtures.ts in place (it is still correct and matches the spec's literal ask, and does not make anything worse), did not remove the manual tracing calls myself since that contradicts the spec's explicit Decision 3 ('do not change what teardown does, only guarantee it always runs'). Did not touch item 2 (docs/agents/BUILDER.md) or item 3 (docs), and did not commit any code changes -- only the earlier STATUS.md move-to-In-Progress commit exists so far.

## Written by
`scripts/needs-human.ts`, 2026-09-13T19:09:16.000Z
