# NEEDS HUMAN: spec 15 item 2/acceptance-criteria

## What is needed
A decision on e2e/settings-push.spec.ts's real-subscribe test, per spec 15's own Risks section: either (a) accept a documented CI-only/environment-only skip for this test with the reason recorded, or (b) have it run headed (not headless) instead of headless in this environment, or (c) some other resolution Eric chooses. The spec explicitly reserves this decision and forbids the builder from loosening the assertion or timeout unilaterally.

## What the session did before stopping
Spec 15 items 1-5 are all built and correct (playwright.config.ts channel:chrome, e2e/fixtures.ts persistent-context, all 8 spec files' import lines, CI workflow's google-chrome --version step, docs) -- confirmed by re-reading every diff against the spec text. lint/typecheck/unit tests (635) and next build all pass. The full e2e suite (npm run test:e2e) ran against a real production next start server with real installed Chrome: 28/29 non-skipped tests passed on the first full run. The one failure was e2e/settings-push.spec.ts's real-subscribe test, which is the exact test this spec exists to fix. Investigated directly (not just re-run blindly): wrote a standalone script driving the identical flow (magic-link auth, grantPermissions, click Enable, poll for Enabled on this device.) outside the Playwright test runner, with console/network/pageerror listeners attached. Findings: (1) the app itself is not broken -- when it works, subscribeToPush's server action, the DB write, and the UI update via revalidatePath all complete correctly within 2-3 seconds, and the written push_subscriptions.endpoint is a real https://fcm.googleapis.com/... URL, satisfying the spec's own new assertion. (2) The failure is that registration.pushManager.subscribe() itself -- the browser's real GCM/FCM registration handshake, which happens at the browser-process level and is invisible to Playwright's page-level request/response listeners -- sometimes does not resolve within 15-20 seconds. (3) Reproduced the flakiness directly with npx playwright test e2e/settings-push.spec.ts run twice back to back per the spec's own acceptance criterion: pass, then fail, on a fresh per-test profile each time (ruling out cross-test state leakage -- this is not a fixture-isolation bug). (4) Tested with headless:false (headed) as a diagnostic per the spec's own Risks section, which names headed-vs-headless GCM behavior as a candidate factor: still hung once out of two headed attempts, so headed alone did not reliably fix it in the limited sample tried. This matches the spec's own named, unconfirmed risk almost exactly (a headless/real branded Chrome talking to Google's real push infrastructure behaving unreliably in this environment), except it is reproducing locally, not only in a hypothetical CI run. Per the spec's own instruction (Risks section, second paragraph), stopping here rather than loosening the test's assertion or timeout myself. Left playwright.config.ts, e2e/fixtures.ts, and all import-line changes uncommitted and exactly as built (verified correct); nothing needs to be undone.

## Written by
`scripts/needs-human.ts`, 2026-09-13T00:34:25.071Z
