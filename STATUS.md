# STATUS — gazelle kanban

**Actual build order so far: 00 → 01 → 02 → 12a (pulled forward) → 03 → 04 → 05 → 06 → 13 (pulled forward) → 07 → 03-rework (addendum) → 07-calendar-fields (addendum) → saved-confirmation-fix → 08 → 09 → 09-review-fixes (addendum) → 14 (pulled forward) → 14-review-fixes (addendum) → 10 → 15 (pulled forward) → 15-review-fixes (addendum) → 16 → 18 (pulled forward) → 19 (pulled forward).** `docs/BUILD_PHASES.md` carries the same note and the reasons 12a, 13, 14, 10, 15, 16, 18 and 19 all moved (10, 15, 16, 18 and 19 each built where the queue already had them; the note is here for the pattern's sake). **Next: spec 17 first-fine-tuning-pass, then spec 11 weekly-planning-and-invites (draft spec first).**

**Spec 19 reviewed clean 2026-09-13.** The reviewer's `REVIEW-FLAGS.md` (diffed against base tag `spec-18`) carried only `note` findings, no `blocking` ones: all four acceptance criteria confirmed evidenced (the fixture's context creation confirmed diff-clean of manual `tracing.start()`/`stop()` calls, `REVIEW.md`'s named `trace.zip` path/size, `docs/agents/BUILDER.md`'s new foreground-verification paragraph, and the before/after tracing error cross-checked independently against the now-deleted `NEEDS_HUMAN.md` recovered at commit `9e9af9f` — same error text, same location); no Medium-tier items (both scope items Low tier); nothing outside scope touched (diff limited to `CHANGELOG.md`, `REVIEW.md`, `STATUS.md`, `docs/BUILD_PHASES.md`, `docs/agents/BUILDER.md`, `docs/specs/19-loop-and-fixture-fixes.md`, `e2e/fixtures.ts`); no hard-rule or convention violation. One trivial note: `STATUS.md`'s "In Progress" bullet incidentally lost its leading `- `, not called for by the spec's own scope, not blocking. Per `docs/agents/MANAGER.md`, only a `blocking` finding requires a review-fixes addendum, so none is drafted. Decision recorded here; `REVIEW-FLAGS.md` deleted.

**CI run `34783413974` (the manager's post-review push of `spec-19` to `origin/main`) is direct, live confirmation this spec's fixture fix actually works, not just a passing local run.** Two tests — `e2e/communities.spec.ts:181` and `e2e/feed.spec.ts:471` — each failed once and then passed clean on Playwright's automatic retry. Before this spec, that specific outcome (a genuine retry that succeeds) was structurally impossible: the old context fixture's manual `context.tracing.start()`/`stop()` calls collided with Playwright's own automatic on-first-retry tracing hook on the same context, so any retry threw `tracing.start: Tracing has been already started` and failed again regardless of whether the underlying test would have passed — exactly the failure mode three separate CI runs today (`34774304088`, `34776292413`, `34776820319`, all pre-fix commits) show on `settings-push.spec.ts:89` instead of the real GCM-timeout error. `34783413974` shows no such contamination: both flaky tests' retries ran clean and the one genuine failure that remained (`settings-push.spec.ts:89`, still the documented real-GCM-handshake risk from spec 15) failed on its own actual assertion, not on a fixture artifact.

**Spec 18 reviewed clean 2026-09-13.** The reviewer's `REVIEW-FLAGS.md` (diffed against base tag `spec-16`, the tag actually preceding this spec's build session since 18 was pulled ahead of 17) carried only `note` findings, no `blocking` ones: all nine acceptance criteria confirmed evidenced (criteria 1/3/4 by the two new e2e tests reading rows back with the admin client, criterion 5 by `redoPhase`'s `run_id` scoping, criterion 6 by `isFromCurrentAssessment` plus its unit coverage, criterion 7 by the migration's backfill and `migrate:status` showing 0019 in sync — confirmed independently by the reviewer too, criterion 8 by the full `test:e2e` regression run, criterion 9 by the stated real-`next start`-plus-real-Chrome verification); the one Medium-tier migration item confirmed applied live and still in sync; nothing outside scope built (no back-navigation change, no answer carry-forward, no resume-abandoned-run UI, no delete path); no hard-rule or convention violation, and the new `docs/CONVENTIONS.md#dialogs` entry this spec's own scope called for is in place. Three notes, none blocking: (1) acceptance criterion 2 (dismissing the dialog creates no run) has no dedicated automated test, but the evidence is structurally sound — `start-new-assessment.tsx`'s `onCancel` never calls `startNewAssessment`, the only path that reaches `startRun` — thin but real, not worth an addendum on its own; (2) `app/(app)/communities/data.ts#readActivities` picking up `assessment_id` (a necessary ripple from item 6's schema change, not scope creep) went unnamed in `REVIEW.md`'s own "What was built" list — a documentation completeness gap, not a code defect, noted here for future review-writing rather than fixed by an addendum; (3) the single-commit build means red-before-green for `tests/assessment-runs.test.ts` can't be independently re-confirmed from git history now, the same accepted one-commit-per-spec limitation already noted for specs 09/10/13/14/16. Per `docs/agents/MANAGER.md`, only a `blocking` finding requires a review-fixes addendum, so none is drafted. Decision recorded here; `REVIEW-FLAGS.md` deleted.

**Spec 16 reviewed clean 2026-09-13.** The reviewer's `REVIEW-FLAGS.md` (diffed against base tag `spec-15-review-fixes`) carried only `note` findings, no `blocking` ones: all seven acceptance criteria confirmed evidenced directly against the diff, not just `REVIEW.md`'s own account (new tests in `tests/scraping-plan.test.ts`, `tests/ics.test.ts`, `tests/schemas.test.ts`, `tests/feed-occurrences.test.ts`, and `e2e/feed.spec.ts`'s new description/day-click tests; `/calendar`'s pre-existing tests and its `committedOnly` filter both confirmed untouched); the one Medium-tier migration item confirmed applied per `haltBeforeMigration: false`, with the usual accepted one-commit-per-spec caveat on red-before-green already accepted for specs 09/10/13/14; nothing outside scope built; no hard-rule or convention violation. Two additional notes, neither blocking: `lib/scraping/plan.ts` has long contained literal embedded NUL bytes in `hashKeyFor`'s template literal (predates spec 16, confirmed byte-identical at the base tag — makes the file diff as binary, worth a small scoped follow-up fix, not blocking); and two reasonable judgment calls the builder itself flagged (`MonthGrid` not owning the ring-highlight itself; `/feed` gaining its own `?month=` param) neither contradict an acceptance criterion or a stated decision. Per `docs/agents/MANAGER.md`, only a `blocking` finding requires a review-fixes addendum, so none is drafted. Decision recorded here; `REVIEW-FLAGS.md` deleted.

**Spec 15 review-fixes addendum reviewed clean 2026-09-13.** The reviewer's `REVIEW-FLAGS.md` (diffed against base tag `spec-15`, the parent spec's own tag per the suffixed-tag rule) carried only `note` findings, no `blocking` ones: all four acceptance criteria confirmed evidenced (both real CI runs transcribed into `REVIEW.md` job-by-job, the two-runs-not-satisfied wording left unweakened, `STATUS.md`'s stale claim and Waiting-on-Eric entry both corrected, and the CLAUDE.md verification sentence adapted for a docs-only session same as specs 09/14's own review-fixes addenda), nothing outside scope touched, no hard-rule or convention violation, and one process note (the diff range `spec-15..spec-15-review-fixes` incidentally includes `9ab4561` and `b45d7ae`, neither of which is this addendum's own work — worth a future reviewer's awareness, not a defect). Per `docs/agents/MANAGER.md`, only a `blocking` finding requires a review-fixes addendum, so none is drafted. Decision recorded here; `REVIEW-FLAGS.md` deleted.

**Spec 15 review-fixes addendum finished 2026-09-13, tag `spec-15-review-fixes`, built by the loop from `docs/specs/15-review-fixes-addendum.md` only.** Resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 15's review gate: the two CI-requiring acceptance criteria were structurally unreachable by any build session under `loop.config.json`'s `push: false`. No code change — Eric had the manager push `spec-15` directly (per his own decision, recorded in the addendum) and read two real CI runs; this session's one scope item transcribed both into `REVIEW.md`'s new "CI confirmation" section, honestly: the first run showed the Playwright job silently skipping (two missing repository secrets); the second, after Eric restored them, showed the job actually executing — Chrome-version step confirmed, but `settings-push.spec.ts`'s real-GCM-handshake test failed after exhausting its two retries (28 passed / 1 failed / 1 skipped). Both CI-requiring acceptance criteria remain **open**, for a real, once-observed CI-environment risk this spec's own Risks section already anticipated — not a defect in this spec's code, and not yet its three-consecutive-failures fallback threshold. `STATUS.md`'s Waiting on Eric section updated accordingly (the secrets ask is resolved; nothing else is an action item for Eric here). Full account in `REVIEW.md`.

**Spec 15 (e2e-real-chrome) finished 2026-09-13, tag `spec-15`, built by the loop from `docs/specs/15-e2e-real-chrome.md` (no addendum), resumed twice mid-session (once after a builder session ended mid-verification without committing, once after `NEEDS_HUMAN.md`/GitHub issue #10 over `settings-push.spec.ts`'s real-GCM-handshake flakiness, resolved by Eric as option 3 — a bounded retry, not a skip).** No PRD coverage, test infrastructure only, like specs 12a/13/14. All five scope items — see Done below for the full account. This session's own remaining work was wiring Eric's resolution into code (`test.describe.configure({ retries: 2, timeout: 90_000 })`, scoped to `settings-push.spec.ts`'s own `describe` block only) and the full live verification pass. **Not pushed** (`loop.config.json`'s `push` is `false`).

**Spec 10 reviewed clean 2026-09-12.** The reviewer's `REVIEW-FLAGS.md` (diffed against base tag `spec-14-review-fixes`, the tag actually preceding this spec's build session, not the unsuffixed `spec-09`) carried only `note` findings, no `blocking` ones: all nine acceptance criteria confirmed evidenced (mostly via `e2e/people.spec.ts` reading the database back with the admin client), nothing outside scope built, no hard-rule or convention violations, and one process note (`REVIEW.md` never says the words "Medium tier" for the three items the spec itself marks Medium, though the underlying work is fully evidenced — worth tightening next time, not blocking). Per `docs/agents/MANAGER.md`, only a `blocking` finding requires a review-fixes addendum, so none is drafted. Decision recorded here; `REVIEW-FLAGS.md` deleted.

**Spec 10 (CRM) finished 2026-09-12, tag `spec-10`, built by the loop from `docs/specs/10-crm.md` (no addendum).** All eight scope items — see Done below for the full account, including a real unmount-before-render bug (`e2e/people.spec.ts`) found by the required live e2e run, the same class specs 04/07/09 each hit once. **Not pushed** (`loop.config.json`'s `push` is `false`).

**Spec 14 review-fixes addendum reviewed clean 2026-09-12.** The reviewer's `REVIEW-FLAGS.md` for `spec-14-review-fixes` (diffed against base tag `spec-14`) carried only `note` findings, no `blocking` ones: all three acceptance criteria confirmed evidenced, nothing outside the addendum's scope touched, and one named tooling limitation (the reviewer can't independently re-verify `logs/live.log`'s line-count claim, since `logs/` is gitignored and gets truncated on every subsequent iteration) explicitly called out as inherent, not a defect, and consistent with the same standard already accepted for specs 13/14. No addendum needed, per `docs/agents/MANAGER.md`'s rule that only a `blocking` finding requires one. Decision recorded here; `REVIEW-FLAGS.md` deleted.

**Spec 14 review-fixes addendum finished 2026-09-12, tag `spec-14-review-fixes`, built by the loop from `docs/specs/14-review-fixes-addendum.md` only.** Resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 14's review gate: the `npm run loop:once`-with-`logs/live.log`-watched acceptance criterion had no evidence. No code change — this addendum's own session, launched by `scripts/run-spec.sh` as a genuine `loop:once` iteration, directly observed `logs/live.log` grow from 13 to 26 real lines as its own real tool calls happened. Full account, quoted sample and line counts in `REVIEW.md`.

**Spec 14 (live loop log) finished 2026-09-12, tag `spec-14`, built by the loop, resumed mid-session (items 1-3 already committed, including item 3's process-group-kill test resolved by hand by Eric; this session built item 4 and wrote up the account).** No PRD coverage, loop tooling only, like spec 13. `scripts/loop-live.ts` sits in the pipe between each `claude -p --output-format stream-json --verbose` invocation and `$LOG_FILE` in `scripts/run-spec.sh`, formatting one `logs/live.log` line per tool call or text message while echoing every raw input line through unchanged. See Done below for the full account. Its one gap (the `loop:once`-watched acceptance criterion had no direct evidence) is now closed by the 14-review-fixes addendum above.

**Spec 09 review-fixes addendum finished 2026-09-12, tag `spec-09-review-fixes`, built by the loop from `docs/specs/09-review-fixes-addendum.md` only.** Resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 09's review gate: `e2e/evaluations.spec.ts` gained two new tests (parametrized over `'cut'`/`'returning'`) proving the cut/returning guard in `submitEvaluation` never promotes an already-cut or already-returning community to `'returning'`, while `times_visited` still increments. No code change to the guard itself — confirmed correct as written. Full account in `REVIEW.md`.

**Spec 09 (evaluation and push) finished 2026-09-12, tag `spec-09`, built by the loop, resumed mid-session (items 1-2 were already committed; items 3-6 were written but uncommitted when this session started).** All six scope items — see Done below for the full account, including a real production-only bug (the Evaluations page's confirmation UI, unmounted by its own revalidation) found and fixed by the required live e2e run. **Not pushed** (`loop.config.json`'s `push` is `false`).

**Spec 08 (Google Calendar sync) built, tagged `spec-08`, and pushed 2026-09-08.** Drafted and built directly in the manager's own session (not the loop), from `docs/specs/08-google-calendar-sync.md`, approved at its own review gate the same day, no addendum. All eight scope items — see Done below for the full account. Every non-live path is verified against a real production `next start` server, including a real (if deliberately invalid) round-trip to Google's actual OAuth/Calendar API. **The one thing not yet done is the spec's own required live hand-test**, which needs three things only Eric can do in Google Cloud Console: confirm the OAuth consent screen (scoped to `calendar.events`, his account as a test user), register two Authorized redirect URIs (`http://localhost:3000/auth/google/callback` and the deployed domain's own), and add `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` to Vercel (Production and Preview) -- `GOOGLE_CLIENT_ID`/`SECRET` are already in `.env.local`, so nothing else is needed to start. Once those three are done, the hand test is: connect in `/settings`, select a real event, confirm it appears on the real Google Calendar with the right title/time/location, unselect, confirm it's removed. `origin/main` is `e97b15d`, and the `spec-08` tag on origin points at that same commit -- confirmed 2026-09-08.

**Saved-confirmation fix against the spec-07-calendar-fields addendum finished 2026-09-08, no new tag (committed on top of `spec-07-calendar-fields`).** Built directly in the manager's own session, not the loop, from a direct instruction (not a spec file). The Community card's autosaved fields had no success feedback -- a write either failed loudly or said nothing, so a person editing times-visited or rating had no way to tell a blur/change actually took. Fixed with a per-field "✓ Saved" `role="status"` badge (`app/(app)/communities/communities-view.tsx`) that flashes for two seconds after `updateCommunity` succeeds, keyed to which field just wrote so editing one never flashes another's badge. **A same-session attempt to also merge `/calendar` into `/feed` as one page (PRD §2.5) was built, then reverted at Eric's request after review** -- `/calendar` is back exactly as `spec-07-calendar-fields` built it: its own page, its own committed-only filter, its own nav entry, same two-pane grid/Upcoming-list layout. `git log` (commit `01e859d` and its revert) carries the tried-and-rejected reasoning for anyone who wants it; `docs/ARCHITECTURE.md`'s "Saved confirmation fix" section has the current-state summary. **Verified live, not just `next build`:** the full `npm run test:e2e` suite (12/12, back to the same set `spec-07-calendar-fields` shipped plus the new Saved-badge test) passed against a real production `next start` server. Pushed to `origin/main` on Eric's confirmation (2026-09-08).

**Spec 07 calendar/community-fields addendum finished 2026-09-08, tag `spec-07-calendar-fields`, pushed.** Built directly in the manager's own session (not the loop), per `docs/specs/07-calendar-and-community-fields-addendum.md`, settled at the spec 07 review gate from a live hand-test of `/feed` and `/calendar`. All three decisions built — see Done below for the full account, including what changed in `e2e/feed.spec.ts`'s pre-existing "reflected on /calendar" test now that `/calendar` no longer shows an unselected event at all.

**Spec 03 rework addendum finished 2026-09-08, tag `spec-03-rework`.** Built directly in the manager's own session (not the loop), per `docs/specs/03-assessment-rework-addendum.md`, settled at the spec 07 review gate. All four decisions: about-you is now one static section (no LLM call between questions; the interview component is called exactly once, to choose the inventories); `submitAnswer` fires `persona_synthesis` in a new `after()` callback and returns immediately, a pattern documented for the first time at `docs/CONVENTIONS.md#background-work-after-the-response` (with its required failure handling: a `run_log` error row newer than the last result, with no matching row, means the results view shows Retry rather than polling forever); `profiles` migration 0013 adds five nullable Settings dials, editable on `/settings`, overriding the matching assessment answer once touched; `/assessment` adopts the spec 04/05 page-layout split, closing `CONVENTIONS.md`'s own pre-convention note. Verified live against the real gateway (not just `next build`): a production `next start` server served the reworked flow for real, `after()` genuinely ran after the response with no page freeze, and a real gateway failure (this account's `model_settings` row still points `persona_synthesis` at the same dead OpenRouter free-tier slug the 2026-09-07 fix note already flags below) produced exactly the expected `run_log` error row and failure/Retry UI with no `assessments` row written. The success path was not observed live on this account for that reason; it is covered by `tests/assessment-data.test.ts` (a stub of the Supabase query builder, no gateway call) and by `e2e/assessment.spec.ts`'s existing results test, which seeds a real `assessments` row directly. Full detail in `REVIEW.md`.

**Spec 07 finished 2026-09-08, tag `spec-07`.** Built by the loop, resumed mid-session after item 1's migration paused it under `haltBeforeMigration` — see Done below for the full account, including a real bug `e2e/feed.spec.ts` caught live.

**Spec 13 (autonomous runner) finished 2026-09-08, tag `spec-13`.** From here on the loop (`npm run loop` / `loop:once`, `scripts/run-spec.sh`) owns the Next → In Progress → Done transitions below, drafting each spec via the planner agent and building it via the builder agent under `CLAUDE.md`'s tier rule — a person no longer types "continue" between scope items or pastes REVIEW.md into a planning chat by hand. The human's job going forward is: when `NEEDS_HUMAN.md` appears at the repo root (and a matching GitHub issue lands in your email), read it, do what it says, delete the file, commit, and restart the loop. Everything else the loop already resolved on its own.

**Spec 06 finished 2026-09-07 (tag `spec-06`).** All eight items, plus item 0's fix, which turned out to be a bigger finding than the spec assumed — see Done below. REVIEW.md written; review gate not yet run.

**Fixed 2026-09-07: the dead-default-model problem flagged right after spec 06.** OpenRouter's free tier had dropped `minimax/minimax-m3:free` (HTTP 404, "This model is unavailable for free"), the default for **seven of eight** components (`interview`, `persona_synthesis`, `activity_suggestion`, `discovery_extraction`, `event_extraction`, `weekly_planning`, `invite_suggestion` — only `discovery_research` was on Gemini already; a brand-new user's onboarding would have 404'd on the very first `interview` call). All seven moved to `gemini-3.6-flash` in `DEFAULT_MODEL_SETTINGS` (`lib/llm/catalog.ts`), each with a reasoning comment per `docs/CONVENTIONS.md#llm-components` stating plainly that this is a stopgap — "point at something that exists today," not a considered fit-for-task pick — and that it undoes `discovery_extraction`'s old provider-diversity reasoning (spreading joints across two providers so one provider's bad day doesn't end every run), since every component now shares one provider. **Buying OpenRouter credit, or finding another confirmed-working free model, is the real fix** and is not done. **Only the documented default changed — existing users' already-seeded `model_settings` rows are untouched** (`emanstof@gmail.com`'s `event_extraction` row was hand-set to `gemini-3.6-flash` during spec 06's own verification; its other six components may still carry the dead model until re-seeded or changed by hand on `/settings`).

**Spec 05 passed its review gate 2026-09-07.** All six deviations in REVIEW.md accepted as built. One decision came out of the gate: `docs/specs/05-duplicate-names-addendum.md`. **It is built and tagged `spec-05-dedupe` (2026-09-07)**, ahead of spec 06's own scope as it required.

**CI is now fully green end to end. ~~The four GitHub repository secrets... are set...~~ — no longer true; see the 2026-09-13 correction below, found while resolving spec 15's review gate.** This claim stood from spec 12a's closure (2026-09-07) until spec 15's CI run showed otherwise. The Playwright job no longer skips itself and `next build` + `next start` under a real magic-link session runs in CI on every push — **when all five repository secrets are actually present, which they are not right now.** The skip gate in `.github/workflows/ci.yml` stays in place by design, for a fresh clone or a fork with no secrets.

**Correction, 2026-09-13 (spec 15 review-fixes addendum):** pushing `spec-15` to confirm its own CI-requiring acceptance criteria surfaced that `NEXT_PUBLIC_SUPABASE_URL` and `ENCRYPTION_KEY` are **not currently set** as GitHub repository secrets (`gh secret list` confirms only `E2E_USER_ID`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` are present) — the opposite problem from what this file long documented (it named `E2E_USER_ID` as the missing fifth; that one is actually set, and two different ones are now missing instead). The Playwright job's own log (`Check for Supabase secrets` step) confirms the skip: `SKIPPING Playwright: repository secrets not set: NEXT_PUBLIC_SUPABASE_URL ENCRYPTION_KEY`. This means the Playwright/e2e job has, in all likelihood, been silently skipping on every CI run since whenever these two went missing — the run as a whole still reports green because a skipped, `if:`-gated step counts as part of a "successful" job in GitHub's UI, so nobody reviewing a run's overall status would have noticed. How or when they went missing is not established; only that they are missing now. Full account in `docs/specs/15-review-fixes-addendum.md`. See Waiting on Eric below.

**Second correction, 2026-09-13 (same addendum, second push):** Eric restored both secrets, and a later unrelated push (spec 16's draft commit) triggered a real, non-skipped CI run — still not "fully green end to end" as this file long claimed, but for a different reason now: `e2e/settings-push.spec.ts`'s real-GCM-handshake test failed in CI after exhausting its two retries (28 passed / 1 failed / 1 skipped), a real instance of the CI-environment risk spec 15's own Risks section already flagged as credible and unconfirmed. Not a defect in anything built; not yet the spec's own three-consecutive-failures fallback trigger either. Full account in `REVIEW.md`'s "CI confirmation" section.

**Addenda waiting to be read when their spec is drafted:** `docs/specs/05-discovery-addendum.md` (spec 05, read), `docs/specs/05-duplicate-names-addendum.md` (**built, tag `spec-05-dedupe`**) and `docs/specs/06-scheduled-jobs-addendum.md` (**read; the unattended fallback chain it settles is spec 11's, not used yet**). They settle decisions made during specs 02 and 05 and override the one-line descriptions in `docs/BUILD_PHASES.md`. `docs/specs/10-crm-addition-note.md` (**read during spec 10's drafting; deliberately deferred, not folded in — see spec 10's own Decisions and Out of scope**) was a smaller note of the same kind for spec 10.

## Backlog
- [LOOP] spec 11 weekly-planning-and-invites (draft spec first) — scheduled parts follow `docs/specs/06-scheduled-jobs-addendum.md`.
- Read `docs/specs/dojo-and-practice-layer-note.md` before drafting spec 11 — a sharper problem statement and an unscoped "dojo" practice layer that may change what the spec is for.

## Next
- **`settings-push.spec.ts:89`'s real-GCM fallback tracker, updated 2026-09-13.** Two genuine failures on record so far — `34734619475` and `34783413974` — both a real `pushManager.subscribe()` handshake timeout after exhausting retries, the risk spec 15's own Risks section flagged. Three other CI runs today (`34774304088`, `34776292413`, `34776820319`) also failed this test but do not count: all three show `tracing.start: Tracing has been already started`, the pre-spec-19 fixture bug, not the real GCM timeout. Spec 15's CI-only-skip fallback (`docs/specs/15-e2e-real-chrome.md`) requires three consecutive genuine failures with no app-code change in between; that has not happened, so the fallback stays unapplied. `34783413974` is the first run after spec 19's fixture fix, so it is the first run that could start such a streak — the next genuine failure, if it comes with no app-code change since `34783413974`, would be the second of three.
- **Loop-tooling finding, found 2026-09-13 in CI run `34783413974`:** `e2e/feed.spec.ts:471` ("an archived community's events are excluded from the feed; a cut community's are not") failed once on a fixture-seeding error — `Could not seed the fixture events: insert or update on table "events" violates foreign key constraint "events_community_id_fkey"` — then passed clean on retry. A latent seeding-order race in the e2e fixtures (the community row the event references isn't reliably committed/visible before the event insert fires), not the settings-push GCM issue. This is loop tooling (`e2e/`), so per `docs/agents/MANAGER.md` it goes through the loop as its own spec, not fixed directly in a manager session.
- **Loop-tooling finding, found 2026-09-13 during spec 19:** a resumed builder session tried to `git revert` on `e2e/fixtures.ts` to redo a "before" reproduction whose evidence already existed, and was stopped by hand. This is loop tooling, so per `docs/agents/MANAGER.md` it goes through the loop as its own spec, not fixed directly in a manager session.
- **Loop-tooling finding, found 2026-09-13 during spec 19:** a planner session drifted into drafting spec 11 (still in Backlog, unapproved) instead of halting on the already-drafted queue (spec 17 was next). No file was written. This is loop tooling, so per `docs/agents/MANAGER.md` it goes through the loop as its own spec, not fixed directly in a manager session.
 **`run-spec.sh` logged `child setpgid (51009 to 51009): Operation not permitted` on the spec 15 builder launch, found 2026-09-12.** Printed once, mid-run, from the `set -m` job-control setup `run_with_timeout` relies on (see that function's own comment in `scripts/run-spec.sh` about needing each backgrounded job in its own process group so `kill -- -$pid` can take out the whole group on a timeout). The run itself completed normally this time (no timeout was hit, so whether the kill-the-whole-group path still works was not actually exercised), but a `setpgid` failure on this machine — a macOS/zsh job-control quirk, a subshell nesting difference, or something else not yet isolated — means the *next* time a builder session genuinely runs past its wall-clock cap, `run_with_timeout`'s `kill -- -$pid` may only kill the top process and orphan the rest, the exact failure mode spec 13's own acceptance testing already found and designed around once. **Candidate fix, not implemented here:** reproduce deliberately (a throwaway low `timeoutMinutes` against a command that outlives it, the same fixture-based technique spec 13's own acceptance testing used) and confirm whether the process group is actually intact despite the logged error, or add a fallback (e.g. `pkill -g` by a recorded PGID, or re-derive the group via `ps -o pgid=`) if it is not. This is loop tooling, so per `docs/agents/MANAGER.md` it goes through the loop as its own spec, not built directly in a manager session.
- **Real fix still open:** buy OpenRouter credit, or find another confirmed-working free model, for the seven components currently stopgapped onto `gemini-3.6-flash` (see the note above). Until then every component shares one provider, so a bad afternoon on Gemini's free tier affects all of them at once. **Confirmed still biting the E2E_USER_ID account specifically** during the spec 03 rework addendum's live verification (2026-09-08): its `persona_synthesis` row still points at `minimax/minimax-m3:free`, which 404s. Re-seeding or hand-fixing that one account's `model_settings` would let the addendum's success path (not just its failure path) be observed live.
- **Existing accounts' already-seeded `model_settings` rows were not touched by the default-model fix** — only new seeds get `gemini-3.6-flash`. Check `/settings` (or the table directly) for any account onboarded before 2026-09-07 that still carries `minimax/minimax-m3:free` or `z-ai/glm-5.2:free` on a component it actually uses.
- 12a item 2: assessment test DONE (spec 04 item 6). Event selection DONE (spec 07 item 7, `e2e/feed.spec.ts`). Spec 05 deliberately adds no e2e suite — see its REVIEW.md.
- **Shared working-tree problem, found 2026-09-12 during spec 10's build:** the pre-commit hook runs a full `npm run typecheck` over the whole tree, so any manager-session commit (docs, `STATUS.md`) attempted while the loop's builder has its own in-progress, uncommitted files checked out makes the tree uncommittable for every other seat — not just a race on the same file, a whole-tree typecheck failing on someone else's half-finished work. Worked around once by hand (`git stash push -- <path>`, re-apply once the tree is idle) rather than fixed. **Candidate fix, not implemented here:** run the loop in its own `git worktree` on its own branch with its own index, so the builder's in-progress files never sit in the same working tree the manager commits docs/STATUS/tags from. This is loop tooling, so per `docs/agents/MANAGER.md` it goes through the loop as its own spec (like 13 and 14 did), not built directly in a manager session.
## In Progress
- [LOOP] spec 17 first-fine-tuning-pass (`docs/specs/17-first-fine-tuning-pass.md`, drafted and committed 2026-09-13, untagged; migration renumbered 0019 → 0020 2026-09-13 now that spec 18 builds first and takes 0019) — checked against `docs/specs/README.md`'s eight required sections, all present in order. Starts from spec 16 (built and tagged `spec-16`); five polish/correctness items on shipped behavior, no new capability: assessment Back navigation (item 1), `unselectOccurrence` moves to a soft `removed` status behind a confirm dialog instead of deleting the row — migration 0020 adds `removed` to `selection_status`, every committed-only read path must exclude it (item 2), a four-step event-density indicator on `month-grid.tsx`'s day cells, shared by `/feed` and `/calendar` (item 3), and `MiniCard` gains a source-link and a `focus`-community marker (item 4). No CI-requiring acceptance criteria. Queued ahead of spec 11 weekly-planning-and-invites (still in Backlog below, draft spec first). Build session started 2026-09-13.

## Blocked
- (nothing blocking the next spec)

## Waiting on Eric
- **Action needed from Eric before spec 08 can be verified live (see the note at the top of this file):** the three Google Cloud Console steps (consent screen, redirect URIs, Vercel env vars) — `GOOGLE_CLIENT_ID`/`SECRET` are already in `.env.local`.
- ~~Action needed from Eric: add `E2E_USER_ID` as a GitHub repository secret~~ — **done; confirmed set via `gh secret list` 2026-09-13.** Superseded by the bullet below, which found the actual current gap.
- ~~Action needed from Eric, found 2026-09-13 while resolving spec 15's review gate: restore `NEXT_PUBLIC_SUPABASE_URL` and `ENCRYPTION_KEY` as GitHub repository secrets~~ — **done; Eric restored both, confirmed by CI run `34734619475` actually executing the Playwright job's real steps instead of skipping (`gh secret list` no longer shows either as missing).** That run surfaced a different, real problem instead: `e2e/settings-push.spec.ts`'s real-GCM-handshake test failed after exhausting its two configured retries (28 passed / 1 failed / 1 skipped) — the exact risk spec 15's own Risks section flagged as credible and unconfirmed, now observed once, not three-consecutive-failures territory yet. Full account in `REVIEW.md`'s "CI confirmation" section (spec 15 review-fixes addendum). Both CI-requiring acceptance criteria from `docs/specs/15-e2e-real-chrome.md` remain open — not action items for Eric, just an outstanding question the next CI-triggering push (or three consecutive failures triggering the documented CI-only-skip fallback) will answer.
- **Action needed from Eric before spec 09's cron route can be verified live:** generate `CRON_SECRET` (e.g. `openssl rand -base64 32`) and set it in `.env.local` and Vercel (Production and Preview) — he has to know the value himself to add it to Vercel, so a build session cannot do this half. `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` are already set and in active use.
- **Decided by Eric 2026-09-12:** `e2e/settings-push.spec.ts`'s real-subscribe test cannot pass against Playwright's bundled Chromium in any environment — confirmed via two isolated diagnostics (see `REVIEW.md`/`docs/ARCHITECTURE.md`'s spec 09 section): Playwright's default context is Chromium's incognito mode, which has no Push API at all, and even a persistent (non-incognito) context then fails because the bundled open-source Chromium carries no Google API key for real GCM registration. The fix — moving the whole e2e suite to a real installed Chrome (`channel: "chrome"`) — is spec 15 (queued above, pulled forward ahead of spec 11), not a permanent gap. No further decision needed here; spec 15 is the action.

**Search keys: OBTAINED 2026-09-06.** All three are in `.env.local` and all three answered a live call before anything was built against them: `EXA_API_KEY`, `TAVILY_API_KEY`, `SERPER_API_KEY`. Eric reports them set in Vercel; **that has not been verified from this machine** — the deployed app cannot search if any is missing there, and spec 05's verification was done against a local `next start`. One click on the deployed `/communities` settles it. Not GitHub secrets: every spec 05 test is fixture-based with no network.

## Done
- spec 19 loop-and-fixture-fixes — done 2026-09-13, tag `spec-19`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop (no PRD coverage, loop tooling like specs 13/14/15), pulled forward ahead of spec 17 per Eric's direct instruction. Two independent one-file fixes.
  - **Item 1 (`e2e/fixtures.ts`).** The spec's original prescribed fix (wrap `await use(context)` in `try`/`finally`, keep the manual `context.tracing.start()`/`stop()` calls) was applied by an earlier session and did not close the bug — confirmed by re-running the "after" case, which still hit `tracing.start: Tracing has been already started` because Playwright's own automatic on-first-retry tracing hook collides with the fixture's manual calls on any newly-created `BrowserContext` (full diagnosis in the now-resolved `NEEDS_HUMAN.md`/GitHub issue #12, and in the spec file's own 2026-09-13 resolution note). The actual fix, applied by this session: removed the manual `context.tracing.start()`/`stop()` calls entirely; `finally` now only closes the context and removes the temp profile directory. Verified directly, not inferred: a throwaway deliberately-failing test (deleted before committing) run with `npx playwright test <file> --retries=1` against a real `next build`/`next start` — retry #1 failed on its own assertion with no leftover-context error, and a real `trace.zip` was confirmed on disk (`test-results/.../trace.zip`), attached by Playwright's own automatic hook. Full `npm run test:e2e` suite re-run clean afterward: 33 passed, 1 skipped — same count as spec 18's last clean run.
  - **Item 2 (`docs/agents/BUILDER.md`).** Added a paragraph requiring every verification command to run in the foreground, awaited in the same turn, never backgrounded to wait on a notification — the exact failure mode that left spec 15's first build attempt uncommitted (a one-shot `claude -p` session has no later turn for such a notification to land on).
  - Docs: `CHANGELOG.md`, `docs/BUILD_PHASES.md`'s build-order line, this file's top-of-file build-order line and Next section (both resolved bullets removed).
  - Verification: no web route to serve (loop tooling, like specs 13/14/15) — verified via the real `npx playwright test`/`npm run test:e2e` runs above, `npm run lint`/`typecheck`/`test` all green.
- spec 18 assessment-runs — done 2026-09-13, tag `spec-18`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/18-assessment-runs.md` (no addendum), pulled forward ahead of spec 17 as planned (spec 17's own Back-navigation item depends on this spec's run-scoped answer reads). Resumed mid-session: all seven scope items were already written, uncommitted, when this session started; each was independently verified against the spec's scope and acceptance criteria rather than trusted, then committed and tagged. All seven scope items.
  - **Item 1 (migration 0019, schemas).** `assessment_runs` table with RLS mirroring `assessment_answers`; `assessment_answers.run_id`/`assessments.run_id` (backfilled, then `not null`); the answers' unique key moved to `(run_id, question_id)` at the DB level; `activities.assessment_id` (nullable). Applied live via `npm run migrate` before this session started (`haltBeforeMigration` is `false`); confirmed still in sync via `migrate:status` at session start and again at the end. `lib/schemas/assessment-run.ts` new; `run_id`/`assessment_id` added to the existing row schemas; coverage in `tests/schemas.test.ts`.
  - **Item 2 (`lib/assessments/runs.ts`).** `pickCurrentRun`/`hasEarlierRun` pure, the rest (`listRuns`/`startRun`/`currentRun`/`completeRun`/`readRunAnswers`) wiring Supabase around them, per `CONVENTIONS.md`'s pure/impure split. `currentRun` creates a run when none exists. Tests in `tests/assessment-runs.test.ts`.
  - **Item 3 (run-scoped interview).** Every read/write in `app/(app)/assessment/actions.ts` resolves `currentRun` first; `writeAnswer` matches on `(run_id, question_id)`; `submitAnswer` calls `completeRun` before firing `persona_synthesis`; `redoPhase` deletes only within the current run. `app/(app)/activities/data.ts` and `app/(app)/settings/page.tsx` (the about-you dial defaults, a fourth consumer of `assessment_answers` not named in the spec's own file list, found by grep) both moved onto the current run's answers. Grepped for every remaining `assessment_answers`/`readAnswers` reference at the end of the session — none left unscoped.
  - **Item 4 (start a new assessment).** `startNewAssessment` server action; new `app/(app)/confirm-dialog.tsx` (dismiss does nothing, per `docs/CONVENTIONS.md`'s new Dialogs entry this spec adds); `start-new-assessment.tsx` wires the button + dialog into `interview.tsx` (shown once a previous run exists) and `results.tsx` (always shown). e2e test seeds a completed run, starts a new one, and reads both runs' rows back with the admin client.
  - **Item 5 (history on results).** `loadResultsPhase` scopes the main result to the current run; a new `loadPreviousAssessments` reads every other run's own finished assessment (newest first) with that run's own answers, reusing `answeredSummaries`' existing rendering. Runs with no `assessments` row are excluded. e2e test confirms the collapsed history section and its expanded transcript.
  - **Item 6 (provenance on activities).** `activities.assessment_id`, stamped by `seedFromAssessment` on every row it inserts; `isFromCurrentAssessment` (pure, `lib/activities/plan.ts`) drives a quiet "From your <date> assessment" line on `/activities`. `seedRowsFrom`, `normalizeName` and the cut-stays-cut rule untouched. Unit test in `tests/plan.test.ts`.
  - **Item 7 (tests and docs).** `docs/ARCHITECTURE.md` gained a new "Assessment runs (spec 18)" section and updated table entries; `docs/CONVENTIONS.md` gained the Dialogs entry; `CHANGELOG.md`, this file.
  - **Verified live, not just `next build`.** `npm run lint`/`typecheck`/`test` (662 unit tests) all green; `next build` passed. A real production `next start` server, driven by real installed Chrome (spec 15), served real authenticated requests: `npx playwright test e2e/assessment.spec.ts` — 4/4 passed, including both new spec 18 tests. The full `npm run test:e2e` suite run as a regression check: 33 passed, 1 skipped (the pre-existing, already-documented `CRON_SECRET` gate) — no new failures.
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-18` tag are local only.
- spec 16 feed-calendar-and-summaries — done 2026-09-13, tag `spec-16`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/16-feed-calendar-and-summaries.md` (no addendum). All seven scope items.
  - **Item 1 (migration 0018, schema).** `events.description` (nullable text), applied live via `npm run migrate` (`haltBeforeMigration` is `false`) and confirmed in sync via `migrate:status`. Added to `eventRow` and to `eventInsert`'s `.partial()` list (`lib/schemas/event.ts`); a new "events row schema (spec 16 item 1)" describe block in `tests/schemas.test.ts` covers a realistic row with a description, a null description, and that insert doesn't require it. Added to `mergeEvents`' `WRITABLE` set (`lib/scraping/plan.ts`) with a new backfill test in `tests/scraping-plan.test.ts` proving a re-scrape of a pre-spec-16 row (description null) writes only `{ description: ... }` and nothing else.
  - **Item 2 (ICS description capture).** `lib/scraping/ics.ts`'s new `normalizeDescription` (private) trims, collapses to single spaces, and truncates to 280 characters at a word boundary; wired into the existing `DESCRIPTION` mapping. Two new fixtures (`tests/fixtures/ics/description-escaped.ics`, `description-long.ics`) plus three new tests in `tests/ics.test.ts` (escaped comma/newline, over-280 truncation at a word boundary, null when absent), red before green.
  - **Item 3 (`event_extraction` writes a summary).** `description: z.string().nullable()` added to the output schema, plus one new prompt rule (one sentence, ≤280 chars, never a restatement of title/time/cost/location, null when the page gives nothing beyond a title and a time). No new component, no new model call — rides the existing per-page extraction. No test asserted against `sampleInput`'s companion output, so nothing there needed updating.
  - **Item 4 (card summary rendering).** `feed-view.tsx`'s `Card` renders `card.description` under the community/time line in `line-clamp-2` muted body text, nothing at all when null (no placeholder, no layout shift). `calendar-view.tsx`'s `MiniCard` untouched, per the spec's own decision.
  - **Item 5 (month grid moves onto `/feed`).** New `app/(app)/feed/month-grid.tsx` — `calendar-view.tsx`'s grid, Prev/Next nav and click-to-scroll/notice behavior moved, not reimplemented; it owns the scroll/notice but not the ring highlight on the resolved day's own section (that section belongs to whichever page's own card list renders it), reported back via an `onDayResolved` callback so each caller rings its own list. `parseMonthParam` moved from `calendar/page.tsx` into `lib/feed/occurrences.ts` (now shared, with 4 new tests) since `/feed` needed the same `?month=` parsing `/calendar` already had. `/feed`'s card list is unaffected by which month the grid shows — it still renders the full unfiltered `FEED_WINDOW_DAYS` window, and the grid's day markers use that same unfiltered `byDay` set.
  - **Item 6 (`/calendar` keeps committed-only).** `calendar-view.tsx` now imports `MonthGrid` from `../feed/month-grid` instead of owning its own copy; its `committedOnly` filter, nav entry and Upcoming list are otherwise untouched. Explicitly checked against the reverted `01e859d` merge (`git show --stat`) before starting — this spec does not repeat it: nothing is removed, the two pages still differ by which card set they hand the shared grid.
  - **Item 7 (tests and docs).** `e2e/feed.spec.ts` gained a `FIXTURE_DESCRIPTION` on the existing seeded event and two new tests: a card with a real seeded description renders it while a card with none (`FIXTURE_CUT_TITLE`, structurally checked via `<p>` count) renders no summary element, and a `/feed` day-click scrolls to that day's section (exact match) or the nearest day with a `role="status"` notice ("Nothing scraped..."), mirroring the existing `/calendar` day-click test. `docs/ARCHITECTURE.md` gained a new "Feed calendar and event summaries (spec 16)" section plus a `description` line on the `events` table entry; `docs/BUILD_PHASES.md`'s build-order note updated to include 16; `CHANGELOG.md` gets one line.
  - **Verified live, not just `next build`.** `npm run lint`/`typecheck`/`test` (647 unit tests) all green. `next build` passed. A real production `next start` server, driven by real installed Chrome (spec 15), served real authenticated requests: `npx playwright test e2e/feed.spec.ts` — 10/10 passed, including both new spec 16 tests, only after the migration was applied live (the first run failed loudly with "Could not find the 'description' column," confirming the schema-cache gap rather than masking it). The full `npm run test:e2e` suite run as a regression check: 31 passed, 1 skipped (the pre-existing, already-documented `CRON_SECRET` gate) — no new failures.
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-16` tag are local only.
- spec 15 review-fixes addendum — done 2026-09-13, tag `spec-15-review-fixes`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/15-review-fixes-addendum.md` only. Resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 15's review gate: its two CI-requiring acceptance criteria were structurally unreachable by any build session under `loop.config.json`'s `push: false` — no build session ever pushes, and CI only runs on a push.
  - **The one scope item, no code change.** The manager had already pushed `spec-15` to `origin/main` directly, on Eric's explicit decision, and read two real CI runs (this session did not push or re-trigger anything). This session's job was to transcribe both runs into `REVIEW.md`'s new "CI confirmation" section, honestly. First run (`34730091495`, commit `a91456a`): the Playwright job's real steps were all skipped — two repository secrets (`NEXT_PUBLIC_SUPABASE_URL`, `ENCRYPTION_KEY`) were missing, unrelated to spec 15's own code. Second run (`34734619475`, commit `b45d7ae`, after Eric restored both secrets): the job actually executed — `google-chrome --version` printed a real version (confirming half the CI acceptance criterion), but `e2e/settings-push.spec.ts`'s real-GCM-handshake test failed after exhausting its two configured retries (28 passed / 1 failed / 1 skipped) — a real, once-observed instance of the exact CI-environment risk spec 15's own Risks section had already flagged as credible and unconfirmed, not a defect in this spec's code.
  - **Both CI-requiring acceptance criteria are left open, honestly, not reworded to appear satisfied.** The retry mechanism itself is confirmed working correctly in CI (two retries fired, then failed outright rather than skipping, exactly as designed). The suite being green in CI is not confirmed, and this is not yet spec 15's own pre-committed three-consecutive-failures fallback trigger (this was the first CI run to reach this test at all) — the next CI-triggering push is the next real data point.
  - **`STATUS.md` updated to match reality, not just `REVIEW.md`.** The Waiting on Eric bullet for the two missing secrets is now marked resolved (struck through, same pattern as the earlier `E2E_USER_ID` entry) with the real follow-on finding recorded in its place; the top-of-file "CI is now fully green end to end" claim gained a second correction paragraph rather than being left to read as settled by the first one.
  - **Verified per the CLAUDE.md rule, adapted for a docs-only addendum with no code and no web route, the same way the 09/14 review-fixes addenda were.** No code changed, so no new lint/typecheck/test run beyond what the pre-commit hook already ran (635 unit tests, green, on the STATUS.md-only commit that started this session). The addendum's own contribution — an honest, complete transcription of two real CI runs already gathered by the manager — is the verification its acceptance criteria ask for; there is nothing further to drive live.
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-15-review-fixes` tag are local only.
- spec 15 e2e-real-chrome — done 2026-09-13, tag `spec-15`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/15-e2e-real-chrome.md` (no addendum). No PRD coverage, test infrastructure only, like specs 12a/13/14. All five scope items.
  - **Item 1 (`playwright.config.ts` — `channel: "chrome"`).** Added to the `chromium` project's `use` block, targeting a real branded Google Chrome install instead of Playwright's bundled open-source Chromium, which has no Google API key and so cannot complete real GCM registration (spec 09's own diagnosis).
  - **Item 2 (`e2e/fixtures.ts` — shared persistent-context fixture).** New module overrides the built-in `context`/`page` fixtures: a fresh `chromium.launchPersistentContext` per test against a temp `os.tmpdir()` profile directory (read via `test.info().project.use`, not hardcoded a second time), reproducing `trace: "on-first-retry"` by hand since overriding `context` takes over that responsibility, and removing the temp directory at teardown (wrapped in try/catch — a just-closed Chrome profile can briefly hold a lock file open). `settings-push.spec.ts` gained a new assertion that the created row's `endpoint` is a real GCM/FCM `https://` URL, and — resolving `NEEDS_HUMAN.md`/GitHub issue #10 (Eric's decision, option 3) — its own `describe` block sets `test.describe.configure({ retries: 2, timeout: 90_000 })`, since the real browser-level `pushManager.subscribe()` handshake outside the app's own code sometimes doesn't resolve in 15-20s even though `subscribeToPush` itself is correct; after exhausting retries the test still fails outright, confirmed live by deliberately breaking the assertion and watching it retry to "Retry #2" before failing.
  - **Item 3 (every existing spec file's import line).** All eight files' `import { expect, test } from "@playwright/test"` became `from "./fixtures"` (`cron-evaluation-prompts.spec.ts` kept its separate `request` import unchanged — it never launches a browser).
  - **Item 4 (CI verifies system Chrome).** `.github/workflows/ci.yml`'s `e2e` job drops `npx playwright install --with-deps chromium` and gains a `google-chrome --version` step immediately before `next build`, relying on `ubuntu-latest`'s preinstalled Chrome rather than a `playwright install chrome` step that wouldn't reliably provision anything on Linux. **Not confirmed by a real CI run this session** — `loop.config.json`'s `push` is `false` and this session never pushes, so nothing triggered GitHub Actions; see `REVIEW.md`'s "What I was unsure about."
  - **Item 5 (docs).** `docs/CONVENTIONS.md#tests` and `docs/ARCHITECTURE.md`'s "Evaluation and push" section both record the fix; `CHANGELOG.md` gets one line.
  - **Verified live, not just `next build`:** the full `npm run test:e2e` suite passed twice fresh (29 passed / 1 skipped each time — the skip is the unrelated, already-documented `CRON_SECRET` gate) against a real production `next start` server driven by a real installed Chrome; `npx playwright test e2e/settings-push.spec.ts` twice back to back both passed; no `gazelle-e2e-*` temp directories remained under `os.tmpdir()` after either clean run (one stale directory from an earlier interrupted session was found and removed before the first clean run); `npm run lint`, `npm run typecheck`, and `npm run test` (635 passed / 4 pre-existing skips) all clean. Full account, including a minor observation about temp-profile cleanup under a deliberately-forced retry-exhaustion run (not the normal suite), in `REVIEW.md`.
- spec 10 crm — done 2026-09-12, tag `spec-10`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/10-crm.md` (no addendum). All eight scope items.
  - **Items 1-2 (`/people` reads, gate, add/edit/archive).** `app/(app)/people/data.ts` follows the spec 04/05 five-file shape, reusing `../feed/data.ts`'s `readEvents`/`readCommunitiesById`/`readSelections` and `../communities/data.ts`'s `readCommunities` directly rather than a second query against those tables — `readMeetableEvents` reads `readSelections`'s own rows for `status: 'attended'` rather than re-expanding recurrence via `expandOccurrences`, since a contact's `met_at_event_id` references an event, not a specific occurrence, and the expansion helper's `MAX_OCCURRENCES_PER_EVENT` cap (walked forward from an event's own `starts_at`) would silently miss an attendance far enough into a long-running recurring event's history. `/people` gates on `hasSelectedActivities`, same reasoning every other post-onboarding page uses. `createContact`/`updateContact` (`actions.ts`) mirror `updateCommunity`'s per-field patch shape exactly; `archiveContact`/`restoreContact` flip `status` — no delete, per `contacts`' own RLS.
  - **Item 3 (interaction logging and tallies).** `logInteraction`'s own input schema accepts only `'text' | 'invite' | 'hangout'` — `'met'` is written in exactly one place, by `createContact`, so a relationship's founding interaction can never be double-counted. `people/view.ts#tally` counts live from `interactions` on every read, never a stored counter, honoring the table's own migration comment.
  - **Item 4 (compose panel).** A fixed, non-LLM template behind `sms:`/`mailto:` links (CLAUDE.md's hard rule: the app only prepares, never sends) and a manual, undetected-by-design "✓ I sent this" confirmation that logs a `'text'` interaction. `invite_suggestion`'s real prompt stays spec 11's.
  - **Item 5 (`lib/crm/import.ts`).** Hand-rolled vCard and CSV parsers, no new dependency — the same call `lib/scraping/ics.ts` made for RFC 5545. A block/row with no name is skipped with a warning naming its position, never guessed at. `tests/crm-import.test.ts` (10 cases, red before green).
  - **Item 6 (import preview and write).** `previewImport` parses and writes nothing; flags a likely duplicate by `lower(btrim(name))` for a person to decide on the review screen, never silently skipped or merged. `importContacts` calls `createContact` once per checked row, `met_on` always null (an import has no real meeting date to invent).
  - **Item 7 (search).** `people/view.ts#matchesQuery`, a pure client-side filter over the page's own already-loaded contact list — no new server round trip. `tests/people-view.test.ts` (8 cases).
  - **A real bug found live, not by the unit suite — the same unmount-before-render class specs 04/07/09 each hit once.** The import review panel called its own `onDone()` in the same tick as showing "Imported N contacts," unmounting the confirmation before anyone could see it. Fixed by not auto-closing the panel on success; the checklist clears but the confirmation stays until dismissed by hand. Full account in `docs/ARCHITECTURE.md`'s new CRM section and `REVIEW.md`.
  - **The viral/k-factor addition note (`docs/specs/10-crm-addition-note.md`) was read and deliberately not folded in**, per the spec's own drafting decision — flagged here and in `REVIEW.md` for the review gate rather than built unilaterally or silently dropped.
  - **Verified per the CLAUDE.md rule, live, not just `next build`.** `next build` passed; `npm run lint`/`typecheck`/`test` (635 unit tests) all green. A real production `next start` server served real authenticated requests: `npx playwright test e2e/people.spec.ts` — 8/8 passed, driving a real vCard/CSV file through a real browser `<input type="file">`, not a stub. The full `npm run test:e2e` suite run as a regression check: 27 passed, 1 failed, 1 skipped — the failure (`e2e/settings-push.spec.ts`) and the skip (cron route's positive path) are the same two pre-existing, already-documented gaps under Waiting on Eric below, unrelated to this spec.
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-10` tag are local only.
- spec 14 review-fixes addendum — done 2026-09-12, tag `spec-14-review-fixes`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/14-review-fixes-addendum.md` only. Resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 14's review gate: the `npm run loop:once`-with-`logs/live.log`-watched acceptance criterion had no evidence, since the sandboxed builder that finished spec 14 couldn't invoke bare `claude` itself.
  - **The one scope item, no code change.** This session's own launch by `scripts/run-spec.sh` is itself a real `npm run loop:once` iteration, with `logs/live.log` truncated fresh at its start and wired through `scripts/loop-live.ts` exactly as spec 14 built it. Directly observed, not inferred: `wc -l logs/live.log` read 13 lines early in the session, then 26 lines after several more real tool calls (two `STATUS.md` edits, a `git commit`, a `grep`/`cat`) — a real quoted sample in `REVIEW.md` shows the growth lines up one-to-one with the real tool calls made, in order, including a genuine `planner`-role phase from the same iteration that ran before this builder session started (direct proof this is a real multi-agent `run-spec.sh` iteration, not a faked pipe).
  - **`REVIEW.md` folded the evidence back into spec 14's own account**, per the addendum's own instruction: its "Verification actually performed" section now has an amendment paragraph stating the gap is closed and pointing at this addendum's section for the detail, rather than describing a real `loop:once` watch as still outstanding.
  - **Verified per the CLAUDE.md rule, adapted for loop tooling (no web route), the same way specs 13 and 14 both were.** No code changed, so no new lint/typecheck/test run beyond what the pre-commit hook already ran on the one `STATUS.md` commit (612 unit tests, green). The addendum's own existence as a real, directly-observed `loop:once` iteration is the verification the acceptance criteria ask for.
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-14-review-fixes` tag are local only.
- spec 14 live-log — done 2026-09-12, tag `spec-14`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/14-live-log.md` (no addendum, no PRD coverage — loop tooling, like spec 13), pulled forward ahead of spec 10 for the same reason 12a and 13 both jumped the queue. Resumed mid-session: items 1-3 were already committed by earlier sessions (including item 3's own required process-group-kill proof, resolved by hand by Eric per the spec's explicit instruction not to re-attempt it); this session verified items 1-3 as left, then built item 4.
  - **Items 1-3, as left by earlier sessions.** Item 1: a real `claude -p ... --output-format stream-json --verbose` capture (`tests/fixtures/loop-live/sample.ndjson`), run by hand by Eric since the bare `claude` command isn't in `.claude/settings.json`'s Bash allowlist — confirms the default (non-partial-messages) shape assumed while drafting the spec. Item 2: `scripts/loop-live.ts` (pure-ish: reads NDJSON from stdin, echoes every line through unchanged, appends a formatted line to `logs/live.log` for each `tool_use`/`text` content block in an `assistant`-typed line, never throws, never exits non-zero) plus `tests/loop-live.test.ts` (13 tests, red before green). Item 3: `scripts/run-spec.sh`'s three `claude -p` invocation sites all gained `--output-format stream-json --verbose` and now pipe through `npx tsx scripts/loop-live.ts` before the existing `>> "$LOG_FILE"` redirect; `logs/live.log` is truncated once near the top of the script, before the planner ever runs. Item 3's required proof that `run_with_timeout` still kills the whole process group with the new pipe stage in place needed bare `bash`/`ps`/`kill`, also outside the allowlist — Eric ran the recipe by hand with a fake `claude` on PATH: `run_with_timeout` returned 124, and a follow-up `ps` showed no leftover `claude`, `npx tsx`/`node` running `loop-live.ts`, or forked child process. Recorded directly in `docs/specs/14-live-log.md` under scope item 3 (dated), not in the repo-root `REVIEW.md`, since that file is overwritten wholesale when the spec finishes rather than read on resume.
  - **Item 4 (docs), this session's own work.** `docs/ARCHITECTURE.md`'s Build loop section gained a "Watching a run live" paragraph; `CHANGELOG.md` and this file's "Actual build order" line (and `docs/BUILD_PHASES.md`'s matching line) both gained the `→ 14 (pulled forward)` segment; `REVIEW.md` overwritten; tag `spec-14`.
  - **How this session verified the acceptance criteria's live-run requirement without invoking `claude`.** The bare `claude` command is outside `.claude/settings.json`'s allowlist for this same unattended builder session — the identical wall items 1 and 3 already hit — so `npm run loop:once` against real `claude` sessions was not attempted. Instead: (1) item 3's own hand-run already exercised the real production pipe (`claude -p ... --output-format stream-json --verbose | npx tsx scripts/loop-live.ts`, fake `claude` standing in) end to end, including a real stream-json line flowing through `loop-live.ts` during a live run, confirmed by Eric's own `ps` check; (2) this session additionally drove the real `scripts/loop-live.ts` process (not `formatEvents()` in isolation) over genuinely streamed stdin — feeding it `tests/fixtures/loop-live/sample.ndjson`'s four lines one at a time with a 150ms delay between each and reading `logs/live.log`'s size after every write — and observed it grow from 0 to 37 bytes after the third line while the child process was still alive, proving lines land on disk as each event is processed, not only at exit. `npm run lint`/`typecheck`/`test` (612 unit tests, `tests/loop-live.test.ts`'s 13 among them) all green.
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-14` tag are local only.
- spec 09 review-fixes addendum — done 2026-09-12, tag `spec-09-review-fixes`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/09-review-fixes-addendum.md` only. Resolves `REVIEW-FLAGS.md`'s one `blocking` finding from spec 09's review gate.
  - **The one scope item.** `e2e/evaluations.spec.ts` gained two new tests (a parametrized loop over `['cut', 'returning']`) proving `submitEvaluation`'s cut/returning guard: seeding a fixture community at `'cut'` or `'returning'`, then a real `attended: true, liked: true` submission through the UI, leaves `status` unchanged (never promoted to `'returning'`) while `times_visited` still increments by one, read back via the admin client. Reused every existing fixture helper unchanged; overrode the fixture's status with a follow-up admin-client `update` after `seedFixture` runs, rather than adding a parameter to `seedFixture` itself, so the shared `beforeEach` the other two tests depend on stays untouched.
  - **No code change to the guard.** Confirmed correct as written, per the addendum's own instruction; only test coverage was missing.
  - **Verified per the CLAUDE.md rule, live, not just `next build`.** `npm run lint`/`typecheck`/`test` (599 unit tests) green. `npm run build` passed. A real production `next start` server served the real authenticated requests both new tests drive: `npx playwright test e2e/evaluations.spec.ts` — 4/4 passed. The full `npm run test:e2e` suite was also run against that server as a regression check: 20 passed, 1 failed, 1 skipped — the failure (`e2e/settings-push.spec.ts`) and the skip (cron route's positive path) are the same two pre-existing, already-documented gaps under Waiting on Eric below, unrelated to this addendum. No new failures.
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-09-review-fixes` tag are local only.
- spec 09 evaluation-and-push — done 2026-09-12, tag `spec-09`, not pushed (`loop.config.json`'s `push` is `false`), built by the loop from `docs/specs/09-evaluation-and-push.md` (no addendum). Resumed mid-session: items 1-2 (migrations 0016/0017, the Evaluations page) were already committed by an earlier session; items 3-6 were written but uncommitted when this session started. All six scope items.
  - **Items 1-2** were reviewed line by line as part of this session's own required verification, alongside the newly-committed items 3-6, and are unchanged from how the earlier session left them.
  - **Item 3 (Web Push core, Settings UI).** `web-push` added as a runtime dependency, pre-approved in the spec itself. `lib/push/notification.ts` (pure) builds the payload; `lib/push/webpush-server.ts` (impure, `PushDeps` matching `GatewayDeps`/`GoogleOAuthDeps`'s shape) sends it, reporting a 404/410 as `{ dead: true }` rather than throwing. `app/(app)/settings/push.tsx` drives the real browser `PushManager`; `subscribeToPush`/`unsubscribeFromPush` (`settings/actions.ts`) upsert/delete the `push_subscriptions` row.
  - **Item 4 (service worker).** `public/sw.js` gained `push` and `notificationclick` listeners — plain service-worker APIs, no new dependency.
  - **Item 5 (cron route).** `lib/supabase/admin.ts` (new, service-role, banner-commented) plus `app/api/cron/evaluation-prompts/route.ts`: `CRON_SECRET`-gated, sends a push per pending-and-unprompted evaluation × subscription pair, deletes dead subscriptions, stamps `evaluation_prompted_at` regardless of outcome. `lib/supabase/proxy.ts#PUBLIC_PATHS` gained `/api/cron` (decision 3). `vercel.json` schedules it once daily.
  - **Item 6 (dynamic hint).** `lib/settings/community-hints.ts` (pure) plus a read in `settings/page.tsx`: "Liked N of M recent visits" next to "Find more communities" once at least two genre-typed `preference_log` rows exist for that focused activity. No automatic trigger — spec 11's territory stays spec 11's.
  - **A real bug found and fixed during required live verification, the same family as specs 04 and 07 each hit once.** `e2e/evaluations.spec.ts`'s two tests failed against a real production `next start` server (not `next build`, not the unit suite): every write succeeded, but the submitted card's "saved, thanks!" confirmation never appeared, because `submitEvaluation`'s own `revalidatePath("/evaluations")` re-rendered the page with a `pending` array that no longer included the just-answered occurrence, unmounting the very card holding the confirmation state before anyone saw it. Fixed by freezing `EvaluationsView`'s render list with `useState(() => pending)` at mount instead of resyncing from the prop on every re-render. Full account in `REVIEW.md` and `docs/ARCHITECTURE.md`'s new spec 09 section.
  - **Verified per the CLAUDE.md rule, live, not just `next build` — with two named gaps, neither an app defect.** `next build` passed; migrations 0016-0017 applied via `npm run migrate`, confirmed via `migrate:status` (0001-0017 all applied); `npm run lint`/`typecheck`/`test` (599 unit tests) all green. The full `npm run test:e2e` suite against a real production `next start` server: 18 passed, 1 failed, 1 skipped. The evaluation-write path (item 2) and the cron route's negative path (item 5) were both exercised live and read back with the admin client. **Not verified live:** the cron route's positive path — `CRON_SECRET` is not yet set anywhere, so that one test skips itself (see Waiting on Eric) — and the push subscribe/unsubscribe round trip (item 3), which cannot be driven by this repository's Playwright setup in any environment, confirmed via two isolated diagnostics narrowing the cause to Chromium's incognito-mode Push API restriction plus the bundled open-source Chromium's missing Google API key (see Waiting on Eric and `REVIEW.md` for the decision this raises for spec 10/11's own e2e coverage).
  - Not pushed, per `loop.config.json`'s `push: false` — commits and the `spec-09` tag are local only; push with `git push origin main spec-09` once `REVIEW.md` is read.
- git-push-follow-tags — done 2026-09-08, no tag, housekeeping fix found while pushing spec-08, built directly in the manager's own session from a direct instruction.
  - **The defect.** `git push --follow-tags` only forwards *annotated* tags; every `spec-NN` tag in this repo (`git tag spec-NN`) is lightweight. Running it after tagging `spec-08` pushed the branch but silently left the tag off `origin` — confirmed by `git ls-remote --tags origin`, which had no `spec-08` until pushed explicitly with `git push origin spec-08`. `loop.config.json`'s own `push` field comment recommended exactly the broken command for a human pushing a `push: false` build by hand, `docs/ARCHITECTURE.md`'s Build loop section repeated the same recommendation, and `scripts/run-spec.sh`'s human-facing log line (when `push=false`) told a person to run it too. **`scripts/run-spec.sh`'s own automated push path (`push: true`) was never actually broken** — it already does `git push` followed by an explicit `git push origin "spec-$spec_num"`, so the loop's own CI-wait step was never at risk; only the advice aimed at a human pushing by hand was wrong.
  - **The fix.** All three human-facing sites now say `git push origin <branch> spec-NN` instead: `loop.config.json`'s `push` comment, `docs/ARCHITECTURE.md`'s Build loop section, and `scripts/run-spec.sh`'s `push=false` log message. No code path changed, since the one place that actually pushes automatically was already correct.
  - Not tagged — a docs/config correction, not a spec.
- spec 08 google-calendar-sync — done 2026-09-08, tag `spec-08`, pushed, drafted and built directly in the manager's own session (not the loop) from `docs/specs/08-google-calendar-sync.md`, approved at its own review gate the same day, no addendum. All eight scope items.
  - **Item 1 (tables).** Migration 0015: `google_accounts` (one connected account per user, email + encrypted access/refresh tokens + expiry, deletable RLS like `provider_keys`) and three nullable columns on `selections` (`gcal_sync_status`/`gcal_sync_error_kind`/`gcal_sync_error_message`), reusing `run_status`/`run_error_kind` verbatim rather than a new enum or a new log table — the same choice `search_log` already made, applied per-row instead of per-call since a sync status is "is this occurrence currently synced," not a call history. `lib/schemas/google.ts` added; `lib/schemas/event.ts`'s `selectionRow` extended. Applied via `npm run migrate`, confirmed via `migrate:status` (0001–0015 all applied).
  - **Items 2–3 (`lib/google/oauth.ts`/`oauth-server.ts`).** Pure/impure pair matching `gateway`/`chain`/`round`. `authorizeUrl` always forces `access_type=offline`+`prompt=consent` so a reconnect can never end up with an access token and no way to refresh it; `signState`/`verifyState` are an HMAC (reusing `ENCRYPTION_KEY`, no new secret) with a ten-minute expiry, the CSRF guard the callback checks before ever exchanging a code. `exchangeCode`/`refreshAccessToken`/`fetchGoogleEmail` take a `GoogleOAuthDeps` (`{ fetch, clientId, clientSecret }`); `serverGoogleOAuthDeps()` reads the real env vars, throwing the same `not_configured` shape `resolveCredentialsFor` does for a missing model-provider key.
  - **Item 4 (`app/auth/google/callback/route.ts`).** Mirrors `app/auth/callback/route.ts`'s shape exactly: a `failure(message)` helper redirecting to `/settings?google=error&message=...` rather than throwing. Verifies `state`, exchanges the code, fetches the connected email, upserts `google_accounts` with both tokens encrypted via `encryptSecret` (the same scheme `provider_keys.key` already uses — second consumer, not a second implementation).
  - **Item 5 (`lib/google/calendar.ts`/`calendar-server.ts`).** `eventBody` builds the Calendar API request body from a small local `CalendarSourceEvent` type (deliberately not importing `FeedCard`, keeping `lib/` independent of `app/`'s route structure); defaults to a one-hour duration when an event has no known end. `createCalendarEvent`/`deleteCalendarEvent` share one `requestWithRefresh` helper: exactly one refresh-and-retry on a 401, the same "one corrective retry, then raise" shape the gateway uses for an invalid model reply. `deleteCalendarEvent` treats a 404/410 (already gone) as success, not a failure — idempotent delete.
  - **Item 6 (wired into `app/(app)/feed/actions.ts`).** `selectOccurrence` checks `hasGoogleAccount` first: none connected leaves all three sync columns null and the returned note unchanged from spec 07's ("Added to your plan.") — no error shown for a feature nobody has opted into. Connected: syncs synchronously (not via `after()` — a single POST is no different in shape from every other single-write action this app already awaits) and writes the outcome to the three sync columns regardless of success or failure; the local `selections` write is never rolled back over a sync failure. A repeat Select on an already-`ok`-synced occurrence skips re-syncing (idempotent, checked via a pre-upsert read). `unselectOccurrence` deletes the Google event first, best-effort — the local delete always proceeds regardless of that outcome. New `retryGoogleSync` re-attempts a failed sync. `app/(app)/feed/data.ts` gained `readEventForSync`, a single-event read (not a reuse of `loadFeedData`'s full join) that recomputes the specific occurrence's own start/end from the event's stored duration, since a recurring event's `starts_at`/`ends_at` describe only its first occurrence.
  - **Item 7 (Settings).** New `app/(app)/settings/google-calendar.tsx` plus `disconnectGoogleCalendar`/`checkGoogleConnection` in `settings/actions.ts`. Not connected: a plain link built from `authorizeUrl`, no server action needed since it's only a redirect. Connected: the account's email (never the tokens, `CONVENTIONS.md#settings-is-the-operator-surface`), Disconnect (a real delete, not status-over-delete — `google_accounts` is credential data like `provider_keys`, not user content), and a "Check connection" button that makes a real refresh call rather than trusting a cached flag, the same reasoning `ModelSettings`' own Test button already has. The origin for the redirect URI comes from the request's own `host`/`x-forwarded-proto` headers (`requestOrigin()` in `page.tsx`), not a hardcoded domain, so every preview deploy gets its own correct callback URL.
  - **Item 8 (card UI).** `feed-view.tsx`'s `Card` and `calendar-view.tsx`'s `MiniCard` both gained a persistent line driven by the stored `gcal_sync_status` (survives a reload, unlike the action's own transient result): "Synced to Google Calendar" on `'ok'`, the real failure message plus a Retry button on `'error'`, nothing at all when null (never attempted).
  - **A named deviation from the drafted spec, decided during the build:** item 3's `GoogleOAuthDeps`/item 5's `CalendarDeps` bundle `clientId`/`clientSecret` directly into the deps object rather than keeping them in a separate structure alongside `{ fetch }` — simpler, since (unlike the LLM gateway, generic over many providers) there is exactly one OAuth provider here, so there was nothing to gain from separating "how to authenticate" from "how to connect."
  - **Verified live per the CLAUDE.md rule, not just `next build` — except the one thing that needs Eric.** The full `npm run test:e2e` suite (14/14) passed against a real production `next start` server, including a new test that seeds a `google_accounts` row with deliberately invalid tokens and drives the real path against Google's real API: a genuine 401, a genuine failed refresh, `gcal_sync_status` landing on `'error'` with a real message, and Retry failing the same deterministic way — all without needing a live user. A direct authenticated check confirmed `/settings` renders the Google Calendar section with a correctly-formed authorize link (right `client_id`/`redirect_uri`/`scope`/`access_type=offline`/`prompt=consent`) and that the callback route redirects with the right error banner on a malformed request. **Not done: the spec's own required live hand-test** (a real OAuth consent grant, a real event appearing on and disappearing from a real Google Calendar) — blocked on three things only Eric can do in Google Cloud Console (see the note at the top of this file and in `REVIEW.md`).
  - **Pushed to `origin/main`** on Eric's confirmation (2026-09-08): `origin/main` is `e97b15d`, and the `spec-08` tag on origin points at that same commit. The tag needed an explicit `git push origin spec-08` -- `git push --follow-tags` alone left it off origin, since it only forwards annotated tags and this repo's spec tags are lightweight; see the `git-push-follow-tags` entry below.
- saved-confirmation-fix — done 2026-09-08, no new tag, built directly in the manager's own session from a direct instruction (not a spec file), on top of `spec-07-calendar-fields`.
  - **The Saved confirmation itself.** The Community card's fields already autosaved -- onBlur for times visited/notes, onChange for status/rating/focus -- with no separate Save button, per `spec-07-calendar-fields`'s own Decision 3. A failure showed a real error; a success showed nothing, indistinguishable from a write that silently never fired. `communities-view.tsx`'s `Card` gained `savedField` state: `save()` now takes the field name it is writing, and on success flashes a `role="status"` "✓ Saved" badge (`SavedBadge`) next to that field for two seconds, cleared via a `setTimeout` ref (cleaned up on unmount). Applied to all five save-backed fields (status, times visited, rating, focus, notes), not just the two named in the request, since they all go through the same `save()` -- confirming two of five and not the rest would have been a stranger result than the shared code path naturally gives. Low tier: UI feedback wired to an already-decided action, no new server logic. `e2e/communities.spec.ts` gained a test driving the badge's appearance and its own two-second self-clear.
  - **A same-session attempt to also merge `/calendar` into `/feed` (PRD §2.5) was built, then fully reverted at Eric's request after review** (`git revert` of commit `01e859d`, itself committed). `/calendar` is back exactly as `spec-07-calendar-fields` built it below: its own page, no `data.ts`/`actions.ts` of its own, the committed-only filter on both the Upcoming list and the month grid's day markers, its own "Calendar" nav entry, and the same two-pane grid/Upcoming-list layout that addendum shipped -- none of that changed. `e2e/feed.spec.ts` is back to the same tests `spec-07-calendar-fields` shipped (below), unchanged by this session.
  - **Verified live per the CLAUDE.md rule, not just `next build`, after the revert.** The full `npm run test:e2e` suite (12/12 -- the same set `spec-07-calendar-fields` shipped, plus the new Saved-badge test) passed against a real production `next start` server.
  - **Pushed to `origin/main`** on Eric's confirmation (2026-09-08), after the review pass above.
- spec 07 calendar/community-fields addendum — done 2026-09-08, tag `spec-07-calendar-fields`, pushed, built directly in the manager's own session (not the loop) from `docs/specs/07-calendar-and-community-fields-addendum.md`, settled at the spec 07 review gate from a live hand-test of `/feed` and `/calendar`. All three decisions.
  - **Decision 1 (`/calendar` committed-only).** New `committedOnly` in `lib/feed/occurrences.ts` filters any array carrying a `selection` field down to `status IN ('planned', 'attended')`; `app/(app)/calendar/page.tsx` applies it to `loadFeedData`'s cards before re-grouping with `groupByDay`, feeding both the Upcoming list and `monthGrid`'s `hasEventsOn` set. `/feed` still reads `loadFeedData`'s cards unfiltered — no change there. Low tier: a narrower filter on the existing join, no new query shape, per the spec's own framing.
  - **Decision 2 (day click scrolls/highlights, never no-ops).** `calendar-view.tsx`'s month-grid cells became `<button>`s with an `onClick` instead of `#day-…` anchor `<Link>`s. An exact day match scrolls its Upcoming section into view and rings it (`activeDay` state). A day with nothing committed calls a new pure `nearestDay` (`lib/feed/occurrences.ts`, ties broken toward the earlier day) and scrolls there instead, showing a `role="status"` notice naming both the clicked day and what it's showing instead — including the all-empty case, where there is nothing to scroll to but the notice still fires. Low tier: pure module plus UI wired to already-decided actions (select/unselect unchanged).
  - **Decision 3 (community visit count and rating).** Migration 0014: `communities.times_visited` (int, not null, default 0) and `rating` (smallint, check 1-5 or null). `lib/schemas/community.ts`'s `communityRow`/`communityInsert` extended; `communities/data.ts` reads both columns; `communities/actions.ts#updateCommunity`'s patch type and validation extended (`z.number().int().nonnegative()` / `.min(1).max(5).nullable()`) — same per-field-write pattern as `status`/`focus`/`user_notes`, so editing one never touches the others. `communities-view.tsx`'s `Card` renders a number input (times visited, onBlur-save like `user_notes`) and a 1-5-or-"Not rated" select (rating, immediate onChange like `status`) next to the existing Status dropdown. Medium tier: new migration, applied via `npm run migrate` and confirmed via `migrate:status` (0001–0014 all applied).
  - **Tests, red before green.** `tests/feed-occurrences.test.ts` gained `committedOnly` (keeps planned/attended, drops skipped/unselected/empty) and `nearestDay` (exact match, nearest-by-distance, tie broken earlier, empty-list null) cases. `tests/schemas.test.ts` gained a community-row fixture with the two new fields, a rating-out-of-range rejection case, and a null-rating acceptance case. Three new/changed `e2e/` cases against the real database and a real production `next start` server: `feed.spec.ts` gained "an unselected event shows on /feed but not /calendar" (committed-only against a real join) and "clicking a day on /calendar scrolls to it when committed, or to the nearest committed day when not" (both the exact-match and empty-day/nearest-day paths, driven through real clicks and `toBeInViewport()`); the pre-existing "reflected on /calendar" test was updated for the new behavior — unselecting on `/calendar` now removes the entry from the list entirely rather than toggling it back to a "Select" button in place, since a card with nothing committed no longer renders there at all. A new `e2e/communities.spec.ts` seeds a fixture focus activity plus community and confirms times-visited and rating write independently (each edit leaves the other two of {times_visited, rating, status} untouched) and survive a page reload, read back with the admin client.
  - **A small necessary wording fix, not scope creep.** `/calendar`'s empty state ("Nothing coming up... scrape a community's calendar") assumed `byDay` was every scraped event; now that it's committed-only, an empty list correctly means "nothing selected yet," so the copy and its link changed to "Nothing on your calendar yet... Select some events on the Feed" pointing at `/feed`. This also required `getByRole("link", { name: "Feed" })` calls in `e2e/feed.spec.ts` to add `exact: true`, since the new link text also contains the word "Feed."
  - **Verified per the CLAUDE.md rule, live, not just `next build`.** `next build` passed, migration 0014 applied to the real project (`npm run migrate`, confirmed via `migrate:status`, 0001–0014 all applied), and a real production `next start` server served real authenticated requests: the full `npm run test:e2e` suite (12/12 — login, assessment ×2, feed ×7 including the three new/changed cases above, communities ×1) passed against that server and the real database, including admin-client read-backs (never inferred from the UI alone) confirming the committed-only filter, the day-click nearest-day resolution, and independent times-visited/rating persistence across a reload.
  - Pushed: commits and tag `spec-07-calendar-fields` on `origin/main`.
- spec 03 rework addendum — done 2026-09-08, tag `spec-03-rework`, built directly in the manager's session (not the loop) from `docs/specs/03-assessment-rework-addendum.md`, reviewed against `docs/CONVENTIONS.md` before starting. All four decisions.
  - **Item 1 (about-you static, one inventory-selection call).** `CONSTRAINT_QUESTIONS` moved from `lib/assessments/flow.ts` into `lib/assessments/catalogue.ts` as `ABOUT_YOU_QUESTIONS` (same five keys, same wording, verbatim, plus four new lead-in questions), and the hobbies/desires LLM phases are gone. `flow.ts`'s `nextStep` returns a `select_inventories` step once every about-you question is answered; `app/(app)/assessment/actions.ts`'s `currentQuestion` makes that one `interview` call inline (framed with `INVENTORY_SELECTION_ASKED_COUNT`/`MAX_QUESTIONS` so the component's existing "last question of the topic" prompt behavior fires without any prompt change) and writes the `about_you:done` marker before the caller ever sees it — so the UI never renders a model-driven step. Low tier: pure module and prompt-wiring changes, no new migration.
  - **Item 2 (interstitial).** Purely client-side in `interview.tsx` — a `useState` flag shown once, the first time an inventory question would render, no server round trip and no stored state, matching `flow.ts`'s "no session state" invariant. Low tier.
  - **Item 3 (background persona synthesis).** `submitAnswer` fires `persona_synthesis` in an `after()` callback (`next/server`) the instant the interview completes, and returns without waiting — a pattern with no precedent in this codebase, reviewed with Eric before starting and written up as a new `docs/CONVENTIONS.md#background-work-after-the-response` entry in the same session, including the required failure handling: the gateway's own `run_log` write (unchanged) is what the results view checks. `app/(app)/assessment/data.ts`'s `loadResultsPhase` decides `results` (an `assessments` row exists) / `failed` (the latest `persona_synthesis` `run_log` row for this user is `error` and no assessment exists) / `cogitating` (neither yet) — checked in that order, so a later successful regenerate always wins over a stale old failure. `generating.tsx`'s `Cogitating` polls via `router.refresh()`; `GatewayFailure` reuses the interview's own error styling and the existing manual "Regenerate" action as Retry. Medium tier: a genuinely new pattern plus a background write path; the required test (`tests/assessment-data.test.ts`, a stub of the Supabase query builder, five cases including "an old error doesn't block a later success") is red-before-green.
  - **Item 4 (Settings dials).** Migration 0013 adds `profiles.dial_budget/sobriety/physical/location/schedule` (nullable text, null = never touched). `lib/activities/plan.ts`'s `constraintsFrom`/`suggestionInputFrom` gained an optional `dials` parameter that wins over the stored answer when non-null (`tests/plan.test.ts` covers both the override and the null-doesn't-blank-it case). `app/(app)/settings/dials.tsx` renders the five fields; `saveDial` writes one field at a time (same per-field-write precedent as spec 05's `status`/`user_notes`/`focus`); "Find more activities" calls the existing `suggestActivities` (spec 04) directly, and "Find more communities" calls the existing `advanceDiscovery` (spec 05) once per currently-focused activity — both are the same actions `/activities` and `/communities` already expose, not new workflow logic. Medium tier: new migration, applied via `npm run migrate` and confirmed via `migrate:status` (0001–0013 all applied).
  - **Adopted the spec 04/05 page-layout split for `/assessment`**, per `docs/CONVENTIONS.md`'s own "Proposed, not yet adopted" note that this was overdue once the route was next touched: added `app/(app)/assessment/data.ts`, leaving `page.tsx` a thin server component.
  - **Verified per the CLAUDE.md rule, live, not just `next build`.** `next build` passed, then a real production `next start` served real authenticated requests: the full `npm run test:e2e` suite (9/9: login, assessment ×2 reworked, feed ×5) passed, and a one-off manual live run (script written, run, then deleted — never committed) against the real gateway confirmed `after()` genuinely fires after the response with no page freeze, and that a real gateway failure (this E2E account's `persona_synthesis` still pointed at the same dead `minimax/minimax-m3:free` slug the 2026-09-07 stopgap note already flags) produced exactly the designed `run_log` error row and the failure/Retry UI with zero `assessments` rows written. The success path was not observed live on this account for that same pre-existing reason; `tests/assessment-data.test.ts` and `e2e/assessment.spec.ts`'s existing results test (which seeds a real `assessments` row and renders from it) cover it instead.
  - No push, per instruction — commits and the `spec-03-rework` tag are local only.
- spec 07 feed-and-calendar-views — done 2026-09-08, tag `spec-07`, built by the loop from `docs/specs/07-feed-and-calendar-views.md` (no addendum). All eight scope items.
  - **Item 1 paused the session under `loop.config.json`'s `haltBeforeMigration`.** Migration 0012 (`selections.occurrence_at`, widening the unique key to `(user_id, event_id, occurrence_at)`) was written and committed, then `NEEDS_HUMAN.md` asked Eric to run `npm run migrate` by hand. He did, confirmed via `migrate:status`, deleted the file and restarted the loop, which resumed at item 2 in the same STATUS.md In Progress entry rather than starting over.
  - `lib/feed/occurrences.ts` (pure, no Supabase/fetch/env): `parseRrule` detects the bounded RFC 5545 subset (`FREQ` DAILY/WEEKLY/MONTHLY, `INTERVAL`, `BYDAY` weekly-only, `COUNT`, `UNTIL`) by grammar, not by which scraper produced the string — prose and anything outside the subset return `null` rather than a best-effort guess. `expandOccurrences` stops at whichever of the window, `COUNT`, `UNTIL`, or `MAX_OCCURRENCES_PER_EVENT` (26) comes first. `groupByDay`/`monthGrid`/`dayKeyIn` back both pages' day-bucketing.
  - `app/(app)/feed/data.ts`'s `loadFeedData` joins events (active only), communities (archived excluded, `cut` still shown, matching `partitionByArchived`'s precedent) and selections into `FeedCard[]`, sorted and pre-grouped by day. `app/(app)/feed/actions.ts`'s `selectOccurrence`/`unselectOccurrence` upsert/delete a `selections` row through the widened unique index — no Google API call, no `gcal_event_id` write, UI copy says "Added to your plan." `/calendar` has no `data.ts`/`actions.ts` of its own; it imports both directly from `../feed/`, per the spec's own drafting decision.
  - **Deviation, flagged during the build:** the spec's own bullet headlines `expandOccurrences` as `(event, window, now)`, but every described cut is expressed entirely through `window.from`/`window.to`, `COUNT`, `UNTIL` and the cap — no described behavior reads a third `now` value. Implemented as `(event, window)`, matching every described behavior and test exactly; `loadFeedData` is what actually injects `now`, using it to build `window.from`.
  - **A real bug found live by `e2e/feed.spec.ts`, before it ever passed, not by unit tests.** Clicking Select genuinely wrote a `selections` row (confirmed independently by querying the table), but neither `/feed` nor `/calendar` ever showed it as selected — not even after a full page reload, which ruled out a Next.js client-cache explanation before the real cause was found. `readSelections` keyed its map on the raw `occurrence_at` string Postgres returns (`"...+00:00"`), while the join looked it up using `expandOccurrences`'s own `occurrenceAt` (`"...Z"`, from `Date#toISOString()`) — two different textual forms of the same instant that never string-matched. Fixed with one shared `occurrenceKey(eventId, occurrenceAt)` helper in `app/(app)/feed/data.ts` that normalizes both sides through `Date` before comparing.
  - Medium tier per CLAUDE.md, on two items: item 1's migration (haltBeforeMigration, above) and item 4's actions.ts (writes `selections` rows) — the latter's own required verification is what item 7's e2e test actually delivered, and where its defect was caught; flagged here since item 4's own commit predated writing the test.
  - **Verified per the CLAUDE.md rule, both halves, live, not just via the suite.** `next build` passed. A real production `next start` served real authenticated requests (via the same admin-minted-magic-link technique as `e2e/login.spec.ts`) that rendered both `/feed` and `/calendar` with a freshly-seeded real event's title present in the response, both 200. The full `npm run test:e2e` suite (login + assessment + feed, 5/5) passed against that same production server and the real database, including a real Select/unselect round-trip read back with the admin client, not inferred from the UI. Not exercised: the deployed Vercel URL.
- spec 13 autonomous-runner — done 2026-09-08, tag `spec-13`, built from `docs/specs/13-autonomous-runner.md` (no addendum), out of order right after spec 06 for the same reason 12a jumped the queue: it changes how every spec after it gets built. All seven scope items, checkpointed the old per-item way as the spec itself required (the last spec built under those rules).
  - Item 1: Supabase CLI (`supabase`, dev dependency) linked to `wqawpwbgrsjusbdopgbi`. `npm run migrate` / `migrate:status` wrap `supabase db push --linked` / `migration list --linked` in a bash one-liner that sources `.env.local`, since the CLI is a Go binary with no `tsx --env-file` support. The dashboard's SQL Editor had already tracked migrations 0001–0010 under its own timestamp-based version ids (not the repo's filenames), and 0011 wasn't tracked at all; reconciled once by reverting those ten stray rows and repairing 0001–0011 in under their real names — bookkeeping only, no schema or data touched. Verified live with a throwaway `0012_noop.sql`: applied by `npm run migrate`, shown as applied by `migrate:status`, then reverted and deleted.
  - Item 2: `CLAUDE.md`'s checkpoint section replaced with the tier rule — one spec per session, Low/Medium/High tier, `NEEDS_HUMAN.md` on High. Six lines added, the old "WAIT for continue" line removed.
  - Item 3: `docs/agents/PLANNER.md`, `BUILDER.md`, `REVIEWER.md` — complete system prompts for headless `claude -p` sessions. Planner drafts a spec only when Next names one with no file yet, commits it untagged, never builds. Builder builds one spec end to end under the tier rule, tags and pushes. Reviewer checks acceptance criteria, Medium-tier flags and Out-of-scope creep against the tag diff, writes `REVIEW-FLAGS.md`, never edits code.
  - Item 4: `scripts/needs-human.ts` writes `NEEDS_HUMAN.md` and opens a matching GitHub issue (title `NEEDS HUMAN: spec NN item M`) using `GH_TOKEN` read directly from the environment, not the ambient `gh` CLI's own auth. `--dry-run` prints without writing. Verified live for real (not just `--dry-run`) during its own build: wrote the file, committed, pushed, and opened `github.com/emanstof-png/social-app/issues/1`, then resolved it (deleted the file, committed, closed the issue) since nothing was actually blocked.
  - Item 5: `.claude/settings.json` (committed) — the unattended-session allowlist: `npm run *`, `npx supabase *`, `npx tsx *`, `npx vitest *`, `npx playwright *`, `git *` except `git push --force*`/`git reset --hard*` (denied), `Edit`/`Write`. `defaultMode: acceptEdits`. Verified live: a real `claude -p` session under this exact config was told to run `curl https://example.com` and reported `DENIED`.
  - Item 6: `scripts/run-spec.sh` (`npm run loop` / `loop:once`) — halt conditions, `git pull --ff-only`, planner, then either stop (a brand-new draft — a person gets a window to reject it before any code exists against it) or build through: builder under a wall-clock cap, wait for CI via `gh`, reviewer. A portable polling-based timeout replaces GNU `timeout` (not on stock macOS). **Two real bugs found and fixed only by testing, before this ever ran for real:** a background-subshell watchdog orphaned a `sleep` process that kept the script's own output redirect open forever after the timed job already finished; and, separately, piping the timed call through `tee` under `set -m` job control killed more of the pipeline than intended and produced the wrong exit code. Verified against a throwaway git+GitHub-Actions-shaped fixture (a local bare "origin" repo, stubbed `claude`/`gh` standing in for the real agents and CI): draft-then-stop, full build-through, a 3-second-capped timeout with no orphaned processes left behind, a pre-existing `NEEDS_HUMAN.md` halting before any agent runs, a reviewer `blocking` flag halting without double-calling `needs-human`, a builder-planted `NEEDS_HUMAN.md` respected rather than re-reported, and a real Blocked-section bullet halting.
  - Item 7: this entry, `CHANGELOG.md`, `docs/BUILD_PHASES.md`, `docs/ARCHITECTURE.md` (Environment: the three loop-only variables and where each lives, plus a full "Build loop" subsection naming the three agents, the tier rule, and the halt conditions), `REVIEW.md`.
  - **Not done, and flagged in `REVIEW.md` and via `NEEDS_HUMAN.md`: the spec's own required live test.** `npm run loop:once` on a real spec (07) was blocked by the auto-mode classifier as a nested `claude -p` session — the right call for an action this size: it would draft spec 07 for real (cheap, local, reversible) but a second invocation would build an entire new feature end to end, migrate the live database, tag and push to `origin/main` (which Vercel auto-deploys from), unsupervised for up to 3 hours. That is a bigger call than building this spec was meant to cover on its own, so it is handed to Eric rather than run silently. Everything else about the loop is built and mechanically verified against the fixture described in item 6.
- default-model stopgap — done 2026-09-07, docs + catalog only, no tag. `DEFAULT_MODEL_SETTINGS` (`lib/llm/catalog.ts`) moved the seven components on the now-dead `openrouter/minimax-m3:free` to `gemini/gemini-3.6-flash`, each with a reasoning comment stating it is a stopgap, not a considered pick, and that it undoes `discovery_extraction`'s old provider-diversity reasoning. Real fix (OpenRouter credit, or another confirmed-working free model) still open — see the note near the top of this file. Existing accounts' already-seeded `model_settings` rows are untouched by this change.
- spec 06 calendar-scraping — done 2026-09-07, tag `spec-06`, built from `docs/specs/06-calendar-scraping.md` with `06-scheduled-jobs-addendum.md` read (its fallback chain is spec 11's, not used here). All eight scope items plus item 0.
  - **Item 0 was not the fix the spec assumed.** The spec's own drafting believed the e2e suite's account (`e2e+gazelle@example.com`) was a throwaway test account colliding with a separate real one. It isn't: git history shows build sessions since spec 02 used its admin-minted magic link as a stand-in for "a real authenticated request" (spec 03's REVIEW.md says so directly — "the dedicated `e2e+gazelle@example.com` account"), and spec 05's real discovery run wrote the 9 real communities and the "Contra dance" activity to it. `emanstof@gmail.com`, the actual account, had 0 activities and 0 communities the whole time. Re-running the (correctly) pinned-account fix reproduced the exact orphaning bug live before the real fix landed: the activity was deleted outright this time, not just orphaned. **Fixed for real, on Eric's confirmation:** migrated the 9 communities and the activity to `emanstof@gmail.com` (`scripts/migrate-real-data-to-emanstof.ts`), advanced its `onboarding_state` to `activities_selected`, and confirmed by re-running the full `npm run test:e2e` suite that the real data survives untouched (9 before, 9 after). `e2e+gazelle@example.com` now owns nothing and is genuinely disposable. `E2E_USER_ID` (created by `scripts/setup-e2e-user.ts`) replaces the old `E2E_TEST_EMAIL ?? "..."` fallback in both `e2e/login.spec.ts` and `e2e/assessment.spec.ts` — no default, fails loudly if unset. CI needs it as a fifth repository secret (see Next).
  - **Also done as part of this session, at Eric's direction:** the two known duplicate `communities` rows (flagged since the spec-05-dedupe gate) archived through the app's own `archiveCommunity`/`updateCommunity` action — driven via a real production session, not a database write — keeping the row with richer discovered fields in each pair, tiebreaking the equally-complete pair by shorter name. Confirmed after: still 9 rows total, the two archived rows' other fields untouched.
  - Migration 0011: `communities.calendar_kind_checked_at` (nullable timestamptz). Applied to `wqawpwbgrsjusbdopgbi` via the Dashboard SQL Editor (this repo has no migration runner — no `supabase` CLI, no `DATABASE_URL`) and verified by a direct column read.
  - `lib/discovery/calendar-kind.ts`/`-server.ts`: a known Meetup/Eventbrite host classifies `api` without a request (their endpoints often refuse an unauthenticated HEAD/GET, and a recognized host should never read "unreachable" because probing failed); `.ics` extension or `text/calendar` content-type is `ics`; anything else that responds is `html`; nothing responding leaves `calendar_kind` null and stamps `calendar_kind_checked_at`.
  - `lib/scraping/ics.ts`: a minimal RFC 5545 `VEVENT` parser — unfolding, TEXT unescaping, `DATE`/`DATE-TIME` (`Z`, `TZID`, or floating, resolved via `Intl`, no timezone-database dependency added), `RRULE` captured raw and never expanded. A malformed block is skipped with a reason; one bad block never loses the rest of the feed.
  - `lib/scraping/plan.ts`/`-server.ts`: `scrapeCommunity` — one pass per community, no round engine (a calendar is one feed or one page fetched once, not an open-ended search, per the spec's own drafting decision against copying spec 05's reducer). `source_url` stamped by this file alone from the URL actually fetched; an ICS `VEVENT`'s own `URL` maps to `rsvp_url`, never `source_url`. `hashKeyFor` is the one place `dedupe_hash` is computed; `mergeEvents` excludes `status` and `scraped_at` from its update diff, which is what keeps a re-scrape with unchanged findings a true zero-write. **Found and fixed before writing tests:** `event_extraction`'s `registration_required` is nullable, but the column is `NOT NULL DEFAULT false` — an insert carrying an explicit null would have failed against the live table; coalesced to `false` at the mapping boundary.
  - `event_extraction`'s real prompt: never invents time/price/location; a recurring pattern is one event with `recurrence` in the page's own words, never an invented series; `event_type`'s three-way meaning for an *event* (as opposed to a community) is this component's own documented judgment call, since nothing else in the repo defines it. Default model: `discovery_extraction`'s existing default and its exact reasoning (per-page volume work, not planning).
  - Communities page: a "Find events" button per community with a `calendar_url`, a calendar-kind badge, an "unreachable" note once detection has failed once. Reuses the existing `ActionResult` shape.
  - **Verified live, both a success and the required failure path, and one real project-wide finding along the way** — see the urgent note above and REVIEW.md for the full account. Not verified live: the ICS path — none of the 9 real communities carry an actual `.ics` URL today (checked). Covered by `tests/ics.test.ts` and `tests/scraping-plan.test.ts` only.
- conventions and spec format — added 2026-09-07, docs only, no tag. `docs/CONVENTIONS.md` is the code schema every spec from 06 onward cites instead of restating. `docs/specs/README.md` is the spec format: three document kinds, the eight required sections in order, scope versus acceptance, how to cite conventions, the drafting process, and spec 05 validated against it as the worked example. `CLAUDE.md` and `docs/BUILD_PHASES.md` point at both.
- spec 05 duplicate-names addendum — done 2026-09-07, **review gate passed the same day**, tag `spec-05-dedupe` (moved to the corrected commit). Both changes: a broader `matchKey` for lookup (leading "the", trailing letters-only parenthetical acronym, separator punctuation) and a second key on the full `normalizeUrl(website)`. `nameKey` untouched, so the app-side key is broader than the index and never narrower. No migration, no dependency, 17 new tests red before green.
  - **`MergePlan.ambiguous` is a new field, not a `dropped` entry.** The addendum said to log a name/website disagreement "in the round's skip reasons", but `dropped` is the not-written list and round.ts prints it as `DROPPED <name>`, which would have logged a community as dropped while writing it. A sibling field and an `AMBIGUOUS` line instead; this is why `round.ts` and `scripts/discover.ts` were touched.
  - **The within-round guard checks the website key too**, which the addendum did not spell out — it describes matching a finding against stored rows. Without it one round can insert two names sharing one URL, the exact duplicate the addendum exists to stop. The real dry run below proved this reachable, not theoretical.
  - **Corrected at its own review gate:** `normalizeUrl` (`lib/search/chain.ts`) now strips a leading `www.`, the exact label only. That is what joins "Silver Spring Contra Dance" (`https://fsgw.org/...`) and "FSGW – Silver Spring Contra Dance" (`https://www.fsgw.org/...`), whose names can never match since there is deliberately no substring containment. Both known pairs are now handled: the FSGW pair by the name key, the Silver Spring pair by the website key. The search chain shares the key, so its hit dedupe now treats www and non-www as one page too. Two pre-existing assertions in `tests/search-chain.test.ts` carried a `www.` through their expected string and were updated; each still asserts its own subject and neither was weakened. Both deviations from the first pass were accepted as built.
  - **Verified per the CLAUDE.md rule, both halves, but the merge path and the page were verified separately.** `next build` passed AND a production `next start` served a real authenticated request that rendered `/communities` (200, real rows, not the signed-out page). The merge logic was exercised by `npm run discover -- --dry-run --user ...` against the 9 real stored communities with real search keys and real models: it collapsed the two FSGW rows into one update instead of two, and dropped four same-round duplicates. The gate's `www.` fix was then confirmed read-only against those same 9 rows: the finding that produced the duplicate now yields 0 inserts, one update, and an `ambiguous` entry naming both stored rows. **No writing discovery run was triggered**, because it would insert another near-duplicate into the table Eric is about to hand-clean. Committed e2e suite (4 tests) passes against the same production server.
  - **Found while verifying, needs a decision before spec 06: the e2e suite and the discovery data share one account.** `e2e/assessment.spec.ts` calls `resetUser`, which deletes `activities` for the e2e user by design. The 9 communities hang off that activity and `communities.activity_id` is `ON DELETE SET NULL`, so running the committed suite orphaned all 9 (`activity_id = null`). Rows, names, websites and source URLs are intact; only the link is gone. Consequence: `npm run discover --dry-run --user ...` no longer finds them and merges against an empty list. **Data repaired 2026-09-07** on Eric's approval: the activity was re-inserted with id `44df58d7-98c2-4cc6-ac58-e0e4cfe9acab` and all 9 communities re-pointed at it (confirmed by read: 9 linked, 0 orphaned). No code changed, so the next `npm run test:e2e` orphans them again — the fix is item 0 of the spec 06 session, under Next.
- spec 05 community-discovery — done 2026-09-06, tag `spec-05`. **Review gate passed 2026-09-07.** All eight scope items. The focus set now has real local organizations under it, each with the URL of the page its facts came from.
  - Migrations 0008 (`search_provider`, `discovery_run_status`, `discovery_extraction` on `llm_component`), 0009 (`discovery_runs`, `search_log`, and `source_url`/`evidence`/`discovery_run_id`/`why_relevant` on `communities`) and 0010 (`discovery_runs.pages_seen`). All applied to wqawpwbgrsjusbdopgbi and verified in `information_schema` and `pg_policies`; both new tables have select/insert/update and no delete.
  - **0010 was not in the spec.** Item 3 requires deduping pages across rounds and item 7 makes each round a separate request, so the already-read set needed persistence, and nothing stored could stand in: `search_log` holds queries, and `communities.source_url` only covers pages that found something. Accepted at the gate.
  - **Gemini Google Search grounding is gone from the codebase**, not just unused: `ChatRequest.googleSearch`, the `google_search` tool push in `callGemini` and the gateway flag are all removed — confirmed at the gate by a repo-wide search returning zero hits. `discovery_research` is `requiresTools: false`. The `tools_unsupported` gate stays for a future component that needs tools.
  - **Verified per the CLAUDE.md rule, both halves.** `next build` passed AND a production `next start` served a real authenticated request that rendered `/communities`, ran real discovery rounds against real search keys and real models, and showed real communities with working source links. Nine communities written, each with a non-null `source_url`. Not yet exercised on the deployed Vercel URL.
  - **Idempotency verified in the table, not the UI.** A community marked `status='went_once'`, `focus=true`, `user_notes='Went on the 5th. DO NOT OVERWRITE.'` still read exactly that after a second real discovery round, which added no duplicate rows.
  - **Known behaviour, found at the gate, not a defect:** "running twice produces zero writes" holds within one run, not across runs. `discovery_run_id` is in `WRITABLE` and is stamped with the current run, so a *second* discovery run over identical findings writes that one field on every matched community. It reads as "which run last confirmed this", no user-owned field is touched and no acceptance criterion breaks, but the merge test asserts the zero-write case with the same run id, so the cross-run case is untested. Leave as is unless spec 07 needs the column to mean "the run that first found this".
  - Known quality issue: one real organization can be written twice under different names. Settled at the gate as `docs/specs/05-duplicate-names-addendum.md`, item 0 of the spec 06 session. The two pairs already in the table are archived by hand.
  - Chain deviation accepted: a 4xx that is not 401/403/429 stops the chain instead of falling through. The three providers have different request shapes, so a 400 from Exa is not necessarily malformed for Tavily; `search_log` will show it if it ever bites, and `classify()` is a two-line change.
- spec 04 activity-selection — done 2026-09-06, tag `spec-04`. All six scope items. The persona is now a short, committed list of activities with a focus set spec 05 can search against.
  - **Spec re-drafted before building.** The original `04-activities-and-focus.md` sketch predated specs 01–03; it was replaced by `docs/specs/04-activity-selection.md`, drafted against what spec 03 actually shipped, and deleted. Nothing in it was dropped.
  - Migration 0006: `activity_kind` enum, `activities.kind` (default `recurring_community`), `activities.fit_score` (0–100, advisory), `profiles.focus_cap` (2–4, default 3). Applied to wqawpwbgrsjusbdopgbi and verified in `information_schema`.
  - **The focus set has no column.** It is derived: `status = 'active'` and `kind = 'recurring_community'`. `communities.focus` stays spec 05's, per-community flag. One idea, one source of truth.
  - **Nothing is ever seeded or suggested active.** The persona's desired activities and every model suggestion arrive `benched`, so the cap holds from the first render and the user picks their own few. This was a decision made while building, not in the spec — see REVIEW.md.
  - Re-running suggestions updates `rationale`, `fit_score` and `kind` only, never `status` or `source`, and writes only fields that actually differ — so a second identical run is a no-op and cannot resurrect something cut.
  - **Corrected at the review gate (migration 0007):** a re-run also leaves `kind` alone once a person has set it by hand (`activities.kind_edited_by_user`). As first specified, item 3 updated `kind` while item 4 made it editable, so a re-run silently discarded the edit. The flag stays false for a model-classified suggestion and for the app's default guess on a seeded row, so a re-run can still correct a guess nobody has reviewed. Reasoning in REVIEW.md and in the spec.
  - `activities_selected` added to `ONBOARDING_STATES`, advanced when the focus set first has something in it. Spec 05 gates on it.
  - **Production-only bug found by the verification rule, not by the build:** two identical GETs in one render are memoized by Next, so re-reading after the seeding insert returned the pre-insert empty list and the page rendered "nothing on your list" on the very render that seeded it. `next build` passed and the rows were really in the database. Fixed by reading the inserted rows back from the insert itself (`app/(app)/activities/data.ts`). Same family as spec 01's `NAV_ITEMS` bug: invisible until a real production request.
  - Verified per the CLAUDE.md rule, **both halves**: `next build` passed AND a production server (`next start`) was driven under a real magic-link session through the whole feature — `/activities` returned 200, seeded cards rendered with their spec 03 rationale, a real `activity_suggestion` run returned 6 constraint-respecting suggestions (free, alcohol-free, knee-aware, weeknights/Saturday, Arlington) with fit scores spread 71–92 and an `ok` run_log row, a second run produced no duplicates and changed no statuses, the focus cap refused a fourth activation, and a **stale-UI replay from a second session was refused by the server**, which is where the rule actually lives. The deployed Vercel URL was not exercised; verification was local `next start`. The committed e2e suite (login + assessment) passes against `next build` + `next start`.
- spec 03 assessment-interview — done 2026-09-06, tag `spec-03`. All six scope items. A signed-in user with configured models now becomes a stored persona: hobbies interview, 1–2 model-chosen inventories, desires and constraints, then a generated assessment.
  - `lib/assessments/catalogue.ts`: four inventories (behavioural profile, short Big Five, core motivations, social style), 43 items, all original wording — no published DISC or Enneagram item, Big Five adapted from the public-domain IPIP pool. Scoring is a pure function, never a model call, and scores are recomputed from the raw answers on every read rather than stored. No migration was needed for this spec.
  - `lib/assessments/flow.ts`: the workflow is deterministic code and derives its position purely from the stored `assessment_answers` rows — no session state, no new table. Question ids carry the phase (`hobbies:<n>`, `inv:<id>:<item>`, `desires:<n>`, `constraints:<key>`).
  - **Design decision worth knowing:** two extra rows, `hobbies:done` and `desires:done`, record that a capped LLM phase closed early and which inventories the model chose. Neither fact is recoverable from question-and-answer rows alone, and the spec rules out session state and a new table. They are filtered out of the transcript, the progress count and the UI. Full reasoning in REVIEW.md.
  - Real prompts for `interview` and `persona_synthesis` (the spec 02 stubs are gone). `suggested_assessments` is a Zod enum of the real catalogue ids, so an invented id fails validation and raises rather than silently leaving the user with no inventory.
  - `assessments.desired_activities` widened from `string[]` to `{name, rationale}[]`. The column is jsonb, so no migration; spec 04 consumes the rationale.
  - Verified per the CLAUDE.md rule, **both halves**: `next build` passed AND a production server (`next start`) was driven through a complete assessment under a real magic-link session — 41 questions, 13 `interview` + 1 `persona_synthesis` run_log rows all `ok`, zero server errors, per-answer writes confirmed, resume-after-reload confirmed, a forced gateway failure surfaced the provider's real HTTP 400 text with a Retry button and an `error`/`provider_error` row in run_log (and no invented question), and redo + regenerate inserted a new `assessments` row with every earlier one retained. Zero server errors in the whole session. The deployed Vercel URL was not exercised for the assessment; verification was local `next start`. The committed spec 12a e2e login suite still passes.
- spec 12a quality-gates — done 2026-09-06, tag `spec-12a`; **fully closed 2026-09-07 when the repository secrets were added.** **Pulled forward out of order**, run between spec 02 and spec 03 instead of after spec 11, because these gates protect specs 03–11 as they land rather than auditing them afterwards; spec 01 had already shipped a production-only bug a gate would have caught. Only 12a moved; 12b–12e stay after spec 11. See `docs/BUILD_PHASES.md` for the same note.
  - GitHub Actions (`.github/workflows/ci.yml`) runs `npm run lint`, `npm run typecheck` and `npm test` on every push and PR; any failure fails the build. Badge in README.
  - Playwright end-to-end login test (`e2e/login.spec.ts`): admin `generateLink` → the app's own `/auth/callback` → authenticated `/settings` renders its real sections, plus signed-out redirect and signed-in `/login` bounce. Runs against `next build` + `next start`, not the dev server.
  - Pre-commit hook (husky + lint-staged): eslint --fix on staged files, then full typecheck and unit suite.
  - **Deferred, both with reasons in `docs/specs/12-professionalize.md`:** Lighthouse CI (every route but `/login` and `/settings` was a placeholder, so a score would have measured the scaffold) and the event-selection e2e test (spec 07). The assessment e2e test landed with spec 04.
  - Two in-scope fixes during the session: `npm run typecheck` now runs `next typegen` first (a clean CI checkout has no `.next/types`, so `LayoutProps`/`PageProps` were undefined and the first CI run failed); and the e2e base URL is `localhost`, not `127.0.0.1`, because Next builds its redirect URL from `localhost` and the mismatch silently dropped the session cookie.
  - **Verified per the CLAUDE.md rule, in CI as well as locally.** The four repository secrets are set, so the Playwright job runs rather than skipping: `next build` + `next start` under a real magic-link session now executes on every push, not only on this machine.
- spec 02 llm-gateway-and-model-settings — done 2026-09-06, tag `spec-02`. Gateway is the single entry point for every model call: resolves the model from `model_settings`, decrypts the key from `provider_keys` (env var as fallback), calls the provider, validates against the component's Zod output schema, retries once on invalid output, writes one `run_log` row per run whether it succeeds or fails. Settings page has provider keys, live per-component model dropdowns with a tools badge, run log with filters and cost/latency, and rerun-with-another-model side by side. Model setup is onboarding step 1 and gates `/assessment`. Migration 0005 added provider/status/error_kind/error_message/attempts/rerun_of to `run_log`.
  - Both rotated keys exercised with real API calls, not just auth checks. OpenRouter and Gemini each ran persona_synthesis end to end and wrote a real `run_log` row.
  - OpenRouter free models sit on a shared upstream pool and return 429 while the key is valid: `z-ai/glm-5.2:free` failed that way and was replaced as the default by `minimax/minimax-m3:free`. Keep more than one free model in mind; any of them can go 429 at any time.
  - Gemini's Google Search grounding is quota-blocked on this key: plain calls return 200 in the same second a grounded call returns 429 RESOURCE_EXHAUSTED. Settled and acted on in spec 05, which removed grounding from the codebase.
  - Gemini's ListModels advertises models that 404 for new keys (`gemini-2.5-flash`), so dropdowns are built from live lists plus a Test button that makes a real call.
  - Both findings above are now settled and need no further discussion: grounding and search providers in `docs/specs/05-discovery-addendum.md`, model fallback for unattended jobs in `docs/specs/06-scheduled-jobs-addendum.md`. No billing on any provider; free-tier fallback chains instead.
  - Key rotation closed out: both rotated keys were exercised with real calls, and the old keys are now deleted.
  - Verified per the CLAUDE.md rule: `next build` passed AND a production server served real authenticated requests. Local `next start` returned 200 for `/settings` and `/assessment` under a real magic-link session, and the deployed https://gazelle-psi.vercel.app returned 200 for `/settings` after the push, with both provider keys picked up from Vercel's environment.
- Scaffold docs written
- spec 01 scaffold-and-data-model — done 2026-09-05, tag `spec-01`. Next.js 16 + Tailwind scaffold, PWA shell, Supabase magic-link auth gated in proxy.ts, all 15 tables applied to wqawpwbgrsjusbdopgbi with RLS (security advisors clean), profiles-on-signup trigger, Zod schemas + 60 tests, eight-section app shell. Fully deployed and working end to end.
  - Vercel settings Eric changed to unblock the deploy: build preset switched to Next.js, and Deployment Protection turned off so the production URL is publicly reachable (SSO on production would also have broken the magic-link callback, which hits the domain in a browser with no Vercel session).
  - Git identity: the repo had no user.email, so commits through c72af98 were authored as ericdesktop@Erics-Mac-mini.local and Vercel refused to build them. Repo identity is now emanstof@gmail.com; keep it that way or deploys start failing again.
  - Post-deploy hotfix (41ae60e..): GET / returned 500 in production with "NAV_ITEMS.map is not a function". NAV_ITEMS was exported from nav.tsx, a "use client" module, and imported by a Server Component, so the server got a client-reference proxy instead of the array. `next build` passes and dev does not reproduce it. Constant moved to app/(app)/nav-items.ts; tests/client-boundary.test.ts now fails the build if any "use client" module exports a non-component.
  - Signup trigger confirmed working: 1 auth user, 1 profiles row.
- spec 00 environment-setup — done 2026-09-05. Supabase project created (wqawpwbgrsjusbdopgbi), GitHub repo pushed (github.com/emanstof-png/social-app), Vercel project "gazelle" linked (first deploy failed as expected, no app code yet). Added Gemini as a supported provider (ARCHITECTURE.md, PRD.md updated). Anthropic key skipped/waived from spec's acceptance criteria per user decision — no Anthropic key in .env.local.
