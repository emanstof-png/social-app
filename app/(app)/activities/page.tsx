import { Placeholder } from "../placeholder";

export const metadata = { title: "Activities — gazelle" };

export default function ActivitiesPage() {
  return <Placeholder name="Activities" spec="04" />;
}
