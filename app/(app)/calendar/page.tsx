import { Placeholder } from "../placeholder";

export const metadata = { title: "Calendar — gazelle" };

export default function CalendarPage() {
  return <Placeholder name="Calendar" spec="07" />;
}
