import { Placeholder } from "../placeholder";

export const metadata = { title: "Settings — gazelle" };

export default function SettingsPage() {
  return <Placeholder name="Settings" spec="02" />;
}
