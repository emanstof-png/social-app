# Holding note — spec 10 addition (viral loop)
Fold this into spec 10's actual scope when spec 10 is drafted. This is a holding note, not the spec itself.

- Shareable event page, viewable without an account
- One-tap "I'm in" that creates an account on accept
- Prewritten invite scripts sent via the phone's native share sheet
- Group invites showing "N friends going" as social proof

This is the intended viral/k-factor loop. Flag it to the planning chat when spec 10 starts so it gets scoped properly rather than pasted in as a raw note.
